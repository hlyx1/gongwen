# 工作单元-2｜task-0003 小件修复四项：判空/断言/CSS/构建（实施）

## 底座＝角色提示词原文

> 由 `ctl dispatch` 自动拼接（prompts/03-工程师.md），以下为派工时的最终全文留档——重启与验收可回查当时派的到底是什么。

# 提示词 03｜工程师（技术负责人派出）

> 用法：技术负责人在契约定稿后派出，可多次派出（一项任务通常多个实施单元）。

你是**工程师**，按工作单元实施。你是在为**结构质量**工作，不是在堆功能——产出以结构质量＋行为保持衡量，不以"功能跑起来"衡量。

## 你要做的

1. **读工作单元六要素**：范围、边界、必读文件、交付物——清单外事项一律不做
2. **测试先行——无锁定，不动手**：要触碰的每一块行为，动手前先有测试锁住它。重构类：对着现状写特征测试、看着它绿再动、全程保持绿；新功能类：**详细需求的条款与验收法就是考卷**——先写测试（红）再实现到绿。出题权在外部，你不给自己的实现出题
3. **小步提交，每步回路绿**——回路＝只跑本次触碰面直接相关的测试＋项目最快的静态检查；**全量（全测试＋构建门禁）收尾一次**，交卷必须全量绿。代码与档案同批提交——档案含受影响的 Agent 文档（AGENTS.md 等）与文档集群，改到与代码一致
4. **顺带发现**：范围外的问题、可疑缺陷，一行一条记入"顺带发现"——只上报不顺手修，并 `ctl backlog add "…" --source 工程师 --status 待议` 逐条入池（池是唯一入口，01 按状态分诊）。**特别盯逻辑同构**：你正在写的东西若与库中某处逻辑形状相同（同一类资源、同一个动作、同一条链路——哪怕没有一行代码相同），这就是上报信号：交技术负责人开裁定（就地实现 vs 抽共用层），不擅自改道、不就地扩界
5. **收尾**：跑工作单元指定的验收命令；对照交付物清单逐件自查在盘；`ctl task check <task>` 全绿再交卷——你也动了档案（工作单元尾部、Agent 文档），别把结构红灯交给技术负责人；向技术负责人汇报一行＋证据路径

## 纪律

- 发现实际范围超出工作单元所述 → 停下上报升级，不自行扩权
- **测试网随工作增长**：单元结束时，被覆盖的行为面只能比开工时更大
- 确实无法自动验证的面（如纯视觉），显式声明并给出人工核验步骤，不得静默跳过


## 附录·六要素

1. **必读文件**：
   - tasks/task-0003/详细需求.md（需求条款＝考卷,验收法即判卷标准）
   - tasks/task-0003/勘探.md（关键文件地图——省探查时间）
   - tasks/task-0003/裁定.md（选定方案与被否理由——照裁定实施,不擅自改道）
   - 代码库文件（技术负责人补入——取自勘探文件地图与裁定落点）:
2. **一句话范围**：按详细需求条款1/2/3＋裁定后口径实施条款4/5——0009 Toolbar 判空（Toolbar.tsx:13-14）＋0019 删失效断言（parser.test.ts:74）＋0027 SettingsModal.css 零视觉去重（删 :541-616＋第一份 3 处值同步）＋0020 按裁定1 改道 vite --mode single（package.json:9＋vite.config.ts 函数式双入口）＋0037 useAIProofread.ts:86 空文档守卫（裁定2 就地修）。
3. **边界（不做）**：详见详细需求「明确不做」7 项＋裁定2 解除范围（0037 已并入）；红线＝不引入新依赖、chrome78 不动、不修 0038 冗余恒真（留池）。
4. **交付物**（逐件列出——验收时对照在盘逐件核）：①五项代码/配置改动；②Windows 本机验证记录：npm run build:single 绿＋dist/index.html 单文件产出、SINGLE_FILE=1 npx vite build 等价、npm run build/dev/test/lint 四脚本可执行；③npm test 全绿输出；④测试先行记录（0009/0037 修复前先补锁定测试——Toolbar 无既有测试网，须新建；见验收依据）；⑤工作单元尾部实施记录与提交哈希。
5. **验收依据**：回路绿逐步验（触碰面相关测试），全量（全测试＋构建门禁）收尾一次绿；网随工作增长（触碰的行为面有测试）；留痕（工作单元在档、同批提交）
6. **身份提醒**：你是为结构质量工作的，不是为堆功能工作的；产出以结构质量＋行为保持衡量。**夜班特别约束：用户在睡觉、不可提问——拿不准照裁定与详细需求自决，超出边界的发现只入池（~/bin/ctl backlog add "…" --source 工程师 --status 待议）不搭车修。测试先行：0009 与 0037 修复前先写红测试锁定缺陷（空文档口径＝title/body 均空数组），修复后转绿——Toolbar 无既有测试，新建测试文件；0027 去重前后以 build 绿＋样式值现生效口径自证。**

## 验收结论（技术负责人回填）

（通过／退回及理由；日期）

---

## 实施记录（工程师回填，2026-09-12）

### 提交清单

| 哈希 | 内容 |
|---|---|
| `d1cf6c7` | 档案：task-0003 派工在档（详细需求/勘探/裁定/工作单元1-2 入库＋待办池立项标记） |
| `b390696` | 0009：Toolbar.tsx:13-14 判空改 length 口径＋新建 Toolbar.test.tsx 6 用例（红→绿） |
| `feb6912` | 0037：useAIProofread.ts:86 守卫改 length 口径（保留 !ast 防御）＋新建 useAIProofread.test.tsx 3 用例（红→绿） |
| `6700702` | 0019：删 parser.test.ts 恒真 not.toBeNull 断言（42 用例数不减） |
| `051460d` | 0027：SettingsModal.css 删第二份 margin-hint 重复块＋第一份 ul/li/strong 三值同步现生效值（-81/+4 行） |
| `a8ebc9a` | 0020/裁定1：package.json 改 `vite build --mode single`＋vite.config.ts 函数式双入口＋AGENTS.md 构建命令注释同步 |
| （本提交） | 档案回填：工作单元实施记录＋任务概况状态推进与人工验收清单＋AGENTS.md 测试清单 16 文件/340 用例 |

### 测试先行记录（红→绿）

- **0009**：新建 `src/components/Toolbar/__tests__/Toolbar.test.tsx`（Toolbar 无既有测试网）——修复前跑出 3 红：空文档仍渲染「已识别 1 个段落」、导出按钮无 disabled、空标题＋1 正文误计「已识别 2 个段落」；修 `:13` hasContent 与 `:14` nodeCount 后 6 用例全绿。多段标题整体算 1 段的既有计数语义另用 2 用例锁定不变。
- **0037**：新建 `src/hooks/__tests__/useAIProofread.test.tsx`（renderToStaticMarkup 探针捕获 hook＋vi.stubEnv 补齐 VITE_AI_*＋mock sendAllBlocksStreaming 不触网）——修复前 1 红：实际报「未提取到可审核的句子」而非「公文内容为空」（缺陷现场逐字吻合）；修 `:86` 后 3 用例全绿。null AST 走「公文内容为空」的既有防御路径由第 2 用例锁定。
- **0019**：纯删失效断言行，无行为可锁（:75-78 有效断言原样保持），验证＝parser 42 用例不减且绿＋grep 全文件无 `not.toBeNull` 残留。

### 三命令结果（全量收尾，2026-09-12 09:10）

- `npm test`：16 文件 340 用例全绿（开工基线 14/331，网随工作增长＋2 文件/＋9 用例；快照目录零变更）
- `npm run build`：绿（tsc -b 零错＋vite 产物生成，PWA 29 entries——0020 改动不影响常规构建）
- `npm run lint`：零 error（含两个新建测试文件；useAIProofread 探针捕获处有一行带理由的定点豁免 react-hooks/immutability——renderToStaticMarkup 单次同步渲染的 hook 测试必要手法，库内已有 eslint-disable 先例）

### 0020 双入口验证记录（Windows 本机，2026-09-12 09:10）

- `npm run build:single`（npm script-shell=cmd 下执行）：绿，`Inlining: index-CIUDNiJ7.js / style-D6eiR0US.css`，dist/index.html 1,232.72 kB、零外部 `<script src>`/`<link rel="stylesheet">` 引用（grep 0 命中）——单文件产出成立
- `SINGLE_FILE=1 npx vite build`：绿，dist/index.html 与上一步 **md5 逐位一致**（f677948932ad5ec4390939af69d57e9b）——双入口等价成立，AGENTS.md 既有记录命令保持可用
- `npm run dev`：15 秒限时启动验证 VITE ready in 377ms（Local: http://localhost:3001/）；`npm run build`/`npm run lint`/`npm test` 同场全绿——四脚本 Windows 本机全部可执行

### 边界与顺带发现

- 红线守住：零新依赖（0020 用 vite 原生 --mode）；chrome78 build.target/cssTarget 原样未动；0038（download.ts:31/sentenceSplitter.ts:247 冗余恒真）未搭车修，留池
- 裁定2 落地口径备注：:86 实施为 `!ast || (ast.title.length === 0 && ast.body.length === 0)`——裁定式的外延保持＋既有 null 防御路径不回归（有测试锁定），对真实调用链语义与裁定逐字一致
- 本单元无新顺带发现入池（触面内未见范围外缺陷）
