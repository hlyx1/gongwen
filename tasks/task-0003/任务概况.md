# task-0003｜小件修复四项：判空/断言/CSS/构建

- 状态：待验收
- 来源：用户 2026-09-12
- 用户原话：（澄清时逐字补录——不得转述改写）
  - 「可做：①方向无歧义的 bug 修复（如待办-0009 Toolbar 恒真、0019 断言失效、0027 重复 CSS）…④工程修复（0020 build:single：优先 .npmrc 设 script-shell 方案，避免新依赖）」（夜班交接决策规则①）
  - 0009 现状描述：ast.title 已是数组（types/ast.ts），Toolbar.tsx:14 判 null 恒真，空文档可导出且统计恒显 1 个段落
  - 0020 现状描述：build:single 脚本用 Unix 前缀环境变量语法，npm 默认 script-shell 为 cmd 导致 Windows 失败；修复优先 .npmrc 设 script-shell，避免新依赖
  - 红线：「build.target chrome78 不动」「不引入新依赖（0020 用 .npmrc 方案）」「测试先行、小步提交、代码与档案同批提交、每单元 ctl check 全绿」
- 一句话目标：小件修复四项：判空/断言/CSS/构建

## 单元清单

> 单元先在这里登记再派工（类型：勘探／实施／复核…；工作单元未落盘不得派子会话）。
> 单元状态：未派／已派／完成／退回。

| 单元 | 类型 | 状态 |
|---|---|---|
| 1 | 勘探 | 完成 |

| 2 | 实施 | 完成 |
## 人工验收清单

> 实施完成后填：需要人工的项（浏览器操作/视觉判断/复杂交互），一条一个可观察结果；技术负责人能自己完美验收的（跑命令、看结果、即判定）由他亲验收尾，清单写声明行「- 无需人工验收：哪些已亲验」。

- 免复核：五项 diff 技术负责人验收时已逐处亲核（Toolbar/useAIProofread 修复与裁定逐字一致、package.json+vite.config 改道形态、CSS 删 81 行＋4 行值同步、断言删除 grep 无残留）；非高风险单元（零新依赖、chrome78 原样、无冻结行为改动——0009/0037 属缺陷修复回归设计意图且有红→绿测试网）；0020 改道的等价性以双入口 md5 逐位一致自证，复核边际收益趋零。
- 无需人工验收（工程师已机械亲验）：0009 空文档判空（Toolbar 6 用例红→绿，静态标记断言＝验收法人工步骤的等价机械覆盖）；0037 空文档守卫（3 用例红→绿）；0019 断言删除（parser 42 用例不减＋grep 无残留）；0020 双入口（npm run build:single 绿＋dist/index.html 零外部 js/css 引用＋SINGLE_FILE=1 npx vite build 产物 md5 逐位一致 f6779489…＋npm run build/dev/test/lint 四脚本 Windows 可执行）。
- 0027 视觉零变化：打开设置弹窗「页面边距说明」展开态，DevTools 计算样式比对 ul 的 line-height=1.6/color=#78350f、li 的 margin-bottom=4px、strong 的 color=#92400e 及列表装饰点/code/pre/链接样式与修复前一致（无快照网，详见详细需求条款3 验收人工步骤）。
- 0020 双击离线打开：dist/index.html 双击可在浏览器离线打开使用（单文件产出已机械自证，双击体验属人工）。
- 0020 GitHub Actions：夜班禁 push 无法实跑；裁定1 改道后 CI 零改动零影响（ubuntu 上 `vite build --mode single` 天然跨平台），推送后 deploy.yml:27 与 release.yml:23 待技术负责人复核绿。
