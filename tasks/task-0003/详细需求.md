# 详细需求｜task-0003 小件修复四项：判空/断言/CSS/构建

> 定稿即冻结：后续改动走新裁定（裁定.md），不回改本件。
> 需求条款用列表行书写，每行必带【验收：…】——验收法写 pytest 用例名或人工步骤。
> 完整性由 `ctl check` 机械把关：离开澄清中前，条款行缺验收法、待澄清占位行残留、「明确不做」缺节，均为红。

## 需求条款

- 条款1（待办-0009）修复 Toolbar 判空恒真：`Toolbar.tsx:13` 的 `hasContent` 改为 `ast.title.length > 0 || ast.body.length > 0`（空文档口径＝经 sanitize+parse 后 title 与 body 均为空数组，见 parser.test.ts:60-70 锁定行为）；顺带将 `:14` 的 `(ast.title ? 1 : 0)` 改为等价显式 `(ast.title.length > 0 ? 1 : 0)`（零行为变化，nodeCount 计数语义不变——多段标题仍整体算 1 段，属既有语义不改）。修复后：空文档时「已识别 N 个段落」不显示、导出按钮 disabled。【验收：npm test 全绿｜人工步骤：清空编辑器（或只留空白行）→ 导出按钮置灰且工具栏无「已识别…个段落」文案；输入一行标题 → 按钮恢复可点、显示「已识别 1 个段落」】
- 条款2（待办-0019）删除失效断言：删除 `src/parser/__tests__/parser.test.ts:74` 的 `expect(ast.title).not.toBeNull()`（对数组恒真无保护力；:75-78 已有完整有效断言保持不动，不新增替代断言）。【验收：npm test 全绿（parser 既有用例数不减）｜人工步骤：grep 确认 parser.test.ts 无 `not.toBeNull` 残留】
- 条款3（待办-0027）SettingsModal.css 零行为去重：删除第二份重复块 `:541-616`（含 :541 重复注释 `/* 页面边距说明 */`）；同时将第一份三条规则属性值更新为现生效值——`.settings-margin-hint-content ul`（:482-488）改为 `line-height: 1.6; color: #78350f`、`.settings-margin-hint-content li`（:490-492）改为 `margin-bottom: 4px`、`.settings-margin-hint-content strong`（:516-518）改为 `color: #92400e`；第一份独有 6 条规则（li::before/li::after/code/pre/a/a:hover）原样保留。【验收：npm run build 构建绿｜人工步骤：打开设置弹窗「页面边距说明」展开态，DevTools 计算样式比对 ul 的 line-height=1.6/color=#78350f、li 的 margin-bottom=4px、strong 的 color=#92400e 及列表装饰点/code/pre/链接样式与修复前一致（视觉零变化）】
- 条款4（待办-0020）修复 build:single 在 Windows 失败（package.json:9 的 `SINGLE_FILE=1` Unix 前缀语法在 npm 默认 script-shell=cmd 下不可执行）：按用户预裁方向采用项目 .npmrc 设 script-shell 方案（本机 bash 实测路径 `C:\Program Files\Git\usr\bin\bash.exe`，项目与用户级 .npmrc 均不存在、无冲突），**不得引入新依赖**；硬约束＝两条 GitHub Actions 工作流（deploy.yml:27 `npm run build`、release.yml:23 `npm run build:single`，均 ubuntu-latest）不得因 script-shell 配置而失败——若项目 .npmrc 影响 CI，配套在 workflow 内以 env 覆盖（如 `npm_config_script_shell: /bin/bash`）；备选方案（`vite build --mode single`＋vite.config.ts 改函数式，CI 与贡献者零影响）已存勘探.md 供技术负责人裁定改道，未获新裁定前按预裁方向实施。vite.config.ts:8 的环境变量读取逻辑不动（build.target/cssTarget chrome78 红线不动）。【验收：npm test 全绿｜人工步骤：① Windows 本机 `npm run build:single` 成功产出单文件 dist/index.html 且双击可离线打开；② `npm run dev/build/lint/test/preview` 五脚本在 Windows 本机全部可执行；③ 推送后 GitHub Actions deploy 与 release 两工作流绿（或验收时以 `bash -c` 等价命令在 Git Bash 复核两脚本语义）】

## 明确不做

- 不引入任何新依赖（0020 不用 cross-env；红线）
- 不动 vite.config.ts 的 `build.target: 'chrome78'` 与 `cssTarget: 'chrome78'`（红线）
- 不改 nodeCount「多段标题整体算 1 段」的既有计数语义（0009 只修判空，不做计数口径变更）
- 不在本任务修 useAIProofread.ts:86 同构守卫失效（待办-0037 已入池，是否并入属技术负责人范围裁定）
- 不清理 download.ts:31、sentenceSplitter.ts:247 的冗余恒真判断（待办-0038 已入池，非 bug）
- 不动 A4Page.css 的同名选择器层叠（经核实为正常覆盖写法，非重复）
- 0027 不删第一份独有 6 条规则、不回退三处现生效属性值（视觉零变化为硬口径）

## 待澄清

- 无（夜班不可提问，已全部收敛：0020 方案按用户预裁方向落条款4，备选方案与 CI 影响证据存勘探.md「0020」节及「落点与影响面」供技术负责人裁定改道；0009 计数语义、0027 删法口径均以现状证据收敛为零行为变化方案；0037/0038 顺带发现已入池不占本任务条款）
