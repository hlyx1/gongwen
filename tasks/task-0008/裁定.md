# 裁定｜task-0008 颜色治理：硬编码归 CSS 变量

- 裁定人：技术负责人（夜班，用户不可及——按交接决策规则代拍板，醒后可复核推翻）
- 日期：2026-09-12

## 裁定1｜0018 挂起回池——关联证伪＋可感度为零

- 交接原文「0012（颜色归 CSS 变量，注意 0018 相关联动）」——勘探证伪：文件/链路/关注点三不相交，无实质联动。
- 0018 本体：docx 库 `Math.floor` 截断（index.mjs:9532 实证）致预览 em 与导出截断值差 0.5 twip≈0.009mm≈预览 0.03px 亚像素——**可感度为零，修复无用户价值**。挂起回池：注记「已实证定位＋量化，修复方向＝决策层 metrics.ts 一处量化，但视觉不可感；除非将来与其他缩进工作合批，否则无独立立项价值」。0018 池行回待议。

## 裁定2｜三项方案分叉定案（均采勘探推荐）

- **命名案甲**（status 族＋warning/error 扩展族）：语义清晰、与既有 --color-status-* 惯例（若有）或族命名一致；案乙的 -alt 家族名弱语义否决。
- **形态案 A**（var() 字符串直换、内联 style 机制不动）：最小差异面约 19 行；案 B（CSS 类迁移）留档备将来，本任务不扩。
- **近似色不硬凑**：三状态色 #11AA66/#999999/#d81e06 与既有变量不等——新增变量值＝现值（视觉零变化是硬口径），禁止就近改用近似值。

## 裁定3｜范围钉死：本任务＝DetectionPanel 单组件

- 0052（其余 10 组件同构族）：本任务是重复族首实例，仅落范式与锁定测试模式；其余组件批次治理留池分诊——**不搭车扩面**（每组件都要自己的映射表与基线核对，混批会稀释验收质量）。
- 死规则 0051 不删（留池）；SVG 图标/控制台着色豁免口径照条款。

**方案三问**：行为＝视觉零变化（44 处色值逐一对映，computed 值基线比对验收）；验证＝colorGovernance 锁定测试（反向验牙口）＋三绿＋computed 基线人工步骤；回滚＝git revert 单提交，无可逆面。

## 附记

- 条款4 的 AGENTS.md 变量表补登（14 新＋12 漏登）一并做——文档与实况同步是治理任务的本分。
- 条款5 锁定测试先红后绿（先写测试验牙口再替换）。
