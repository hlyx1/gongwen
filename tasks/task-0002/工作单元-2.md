# 工作单元-2｜task-0002 测试补强：导出快照封口＋AST类型封闭化（实施）

## 底座＝角色提示词原文

> 由 `ctl dispatch` 自动拼接（prompts/03-工程师.md），以下为派工时的最终全文留档——重启与验收可回查当时派的到底是什么。

# 提示词 03｜工程师（技术负责人派出）

> 用法：技术负责人在契约定稿后派出，可多次派出（一项任务通常多个实施单元）。

你是**工程师**，按工作单元实施。你是在为**结构质量**工作，不是在堆功能——产出以结构质量＋行为保持衡量，不以"功能跑起来"衡量。

## 你要做的

1. **读工作单元六要素**：范围、边界、必读文件、交付物——清单外事项一律不做
2. **测试先行——无锁定，不动手**：要触碰的每一块行为，动手前先有测试锁住它。重构类：对着现状写特征测试、看着它绿再动、全程保持绿；新功能类：**详细需求的条款与验收法就是考卷**——先写测试（红）再实现到绿。出题权在外部，你不给自己的实现出题
3. **小步提交，每步回路绿**——回路＝只跑本次触碰面直接相关的测试＋项目最快的静态检查；**全量（全测试＋构建门禁）收尾一次**，交卷必须全量绿。代码与档案同批提交——档案含受影响的 Agent 文档（AGENTS.md 等）与文档集群，改到与代码一致
4. **顺带发现**：范围外的问题、可疑缺陷，一行一条记入"顺带发现"——只上报不顺手修，并 `ctl backlog add "…" --source 工程师 --status 待议` 逐条入池（池是唯一入口，01 按状态分诊）。**特别盯逻辑同构**：你正在写的东西若与库中某处逻辑形状相同（同一类资源、同一个动作、同一条链路——哪怕没有一行代码相同），这就是上报信号：交技术负责人开裁定（就地实现 vs 抽共用层），不擅自改道、不就地扩界
5. **收尾**：跑工作单元指定的验收命令；对照交付物清单逐件自查在盘；`ctl task check <task>` 全绿再交卷——你也动了档案（工作单元尾部、Agent 文档），别把结构红灯交给技术负责人；向技术负责人汇报一行＋证据路径

## 纪律

- 发现实际范围超出工作单元所述 → 停下上报升级，不自行扩权
- **测试网随工作增长**：单元结束时，被覆盖的行为面只能比开工时更大
- 确实无法自动验证的面（如纯视觉），显式声明并给出人工核验步骤，不得静默跳过


## 附录·六要素

1. **必读文件**：
   - tasks/task-0002/详细需求.md（需求条款＝考卷,验收法即判卷标准）
   - tasks/task-0002/勘探.md（关键文件地图——省探查时间）
   - tasks/task-0002/裁定.md（选定方案与被否理由——照裁定实施,不擅自改道）
   - 代码库文件（技术负责人补入——取自勘探文件地图与裁定落点）:
2. **一句话范围**：按详细需求条款1~5 实施：0029＝types/ast.ts 判别联合封闭＋4 处 switch 补标签与 never 穷尽断言＋sentenceSplitter 两表收紧 Record<NodeType,boolean>＋牵连适配（parser 窄化/5 个测试工厂断言/冗余 cast 删除）；0030＝docxBuilder.test.ts 的 packParts 加 settings 字段＋开/关两用例各一行断言。
3. **边界（不做）**：红线＝AST 运行时结构与 sentenceId 生成/切句边界一字不动（0029 仅类型层）；不给 settings.xml 升快照；不收窄下游类型；不搭车修待办-0009；不动 eslint 配置——详见详细需求「明确不做」7 项。
4. **交付物**（逐件列出——验收时对照在盘逐件核）：①代码改动（落点以勘探.md 文件地图为准）；②npm test 全量绿＋npm run build 绿的输出记录；③「临时加假枚举成员→4 处 tsc 报错→撤销」的抽验记录；④工作单元尾部实施记录与提交哈希。
5. **验收依据**：回路绿逐步验（触碰面相关测试），全量（全测试＋构建门禁）收尾一次绿；网随工作增长（触碰的行为面有测试）；留痕（工作单元在档、同批提交）
6. **身份提醒**：你是为结构质量工作的，不是为堆功能工作的；产出以结构质量＋行为保持衡量。

## 验收结论（技术负责人回填）

通过：三命令亲验全绿（331用例/build/lint零error）；快照目录零变更；切句两表逐键取反truthiness等价在diff直证；settings断言两用例符合裁定2值断言形态；假成员抽验8处正向报错记录在案。裁定认可0036处置＝全键分摊（封闭强制力优先，Partial会让新增成员静默通过违背0029目的）；验收人顺笔勘误切句表注释键数笔误（13→12，NodeType实为12成员）。

---

## 实施记录（工程师回填，2026-09-12）

### 提交清单

| 哈希 | 内容 |
|---|---|
| `1cf745c` | 0030：docxBuilder.test.ts packParts 纳入 word/settings.xml＋奇偶页码开/关两用例各一行断言 |
| `4c54e62` | 0029 条款1：DocumentNode 判别联合封闭＋assertNever 辅助＋牵连适配（parser 窄化/5 工厂断言/冗余 cast 删除） |
| `bde598d` | 0029 条款2/3：4 处 switch 补标签＋never 穷尽断言＋切句两表 Record 封闭（含 Exclude 兜底成员） |
| （本提交） | 档案回填：工作单元实施记录＋任务概况状态推进＋AGENTS.md 同步＋顺带发现入池（0035/0036） |

### 三命令结果（全量收尾，2026-09-12 08:46）

- `npm test`：14 文件 331 用例全绿（与开工基线持平，快照目录零变更）
- `npm run build`：绿（tsc -b 零错＋vite 产物生成，PWA 29 entries）
- `npm run lint`：零 error

### 抽验记录（条款2 验收法「临时假成员」）

1. `NodeType` 枚举临时加 `FAKE = 'FAKE'` → `npx tsc -b` 报 **8 处错，全部为预期正向形态**：
   - **4 处 switch 穷尽断言**（条款2 验收目标）：layout/fonts.ts:55、layout/index.ts:141（paragraphAlignment）、layout/index.ts:172（paragraphIndent）、hooks/useDetectionData.ts:182（calculateBodyStats）——均为 `Argument of type 'NodeType.FAKE' is not assignable to parameter of type 'never'`
   - 2 处切句 Record（条款3 验收目标）：sentenceSplitter.ts:54/:74（两张表键义务）
   - 2 处既有封闭 Record（勘探 §1.2-B 正向协同）：renderContentFlow.tsx:23（NODE_CLASS_MAP）、types/detection.ts:137（NODE_TO_DETECTION_MAP）
2. 撤销 FAKE → tsc 恢复零错＋331 全绿（diff 无 FAKE 残留，机械核对）

### 实施要点与判卷材料（技术负责人验收重点）

- **TS 收窄限制的形态适配**（照裁定1 assertNever 方案，两处技术性偏离「标签堆叠进 default」的直觉形态）：
  1. 穷尽断言不可放在空 case 标签堆叠的 default 内（TS 对 fall-through 的收窄含堆叠标签值、非 never）——改为堆叠组显式 `return` 原值＋default 仅剩断言与**同值兜底 return**，逐枚举成员返回值与改前一字不差。
  2. calculateBodyStats 判别值提升为局部变量 `const nodeType = node.type`（switch 直接判属性会把 node 收窄为 never，default 内无法取值）——属性读取次数不变，运行时等价。
- **Exclude 兜底成员**（ast.ts）：`DocumentNodeBase & { type: Exclude<NodeType, 已知12> }`——现状 Exclude=never（兜底不可构造，联合可用值集与显式列举**完全一致**、运行时零变化）；新增枚举成员时 type 域自动跟随 NodeType 全集，保证 useDetectionData 处断言不静默失效（第一次抽验实测该处漏报后补设）。
- **【判卷重点】条款3 两表键集扩大**（已入池待办-0036）：`Record<NodeType, boolean>` 的全键义务与括号「运行时对象不动」互斥（两表原 6/7 键分摊）。按机械验收（「新增成员时两张表 tsc 报错」）优先落**各 12 键布尔分摊**（互为反集）——运行时对象键集 +6/+7 为 Record 字面形态的必然推论；truthiness 分支与改前**逐路径等价**（缺键 undefined 与显式 false 同为 falsy），切句逻辑与 sentenceId 拼接一行未改（红线），全库两表外部消费点为零（已检索），331 用例全绿＋快照零变更为行为等价机械证据。
- **运行时零变更自证**：git diff 全量核对——NodeType 枚举 12 成员零变化；DocumentNode/AttachmentNode/TableNode 改动均为纯类型声明（interface→联合/基形状继承）；assertNever 为空函数体 no-op；4 处 switch 逐成员返回值与改前一致（diff 逐行核对，仅标签/断言/注释增删）；parser.ts 一处 as 断言（编译期，零运行时）；5 个测试工厂各加一行 as（测试文件）；sentenceSplitter 除两表键值分摊外零改动（DELIMITERS/PAIR 表/切句函数/sentenceId 拼接/exports 一字未动）。
- **红演示**（0030 测试先行）：断言先故意写错值（`w:val="true"`）实测红（`expected … to contain` 失败）后落正式形态转绿，证明断言真实在跑；负向断言选**值形态** `w:val="false"`（裁定2 授权二选一）——比 not.toContain 真值形态多锁一层（接线被删导致元素整个消失时同样红）。
- **顺带发现入池**：0035（NodeType 实为 12 成员，需求/勘探档案称 13——数字失实，抽验实测发现）、0036（条款3 内部冲突决策记录）。均只上报未搭车修。
- 人工验收：无需人工验收——三命令＋抽验记录＋diff 核对均可由技术负责人跑命令/看记录判定；纯类型层无视觉面。
