# 详细需求｜task-0002 测试补强：导出快照封口＋AST类型封闭化

> 定稿即冻结：后续改动走新裁定（裁定.md），不回改本件。
> 需求条款用列表行书写，每行必带【验收：…】——验收法写 vitest 用例名或人工步骤。
> 完整性由 `ctl check` 机械把关：离开澄清中前，条款行缺验收法、待澄清占位行残留、「明确不做」缺节，均为红。

## 需求条款

- 条款1（0029·判别联合封闭）：`src/types/ast.ts` 的 `DocumentNode` 由单一 interface 改为按 `type` 判别的封闭联合——11 个同形状成员（DOCUMENT_TITLE/H1-H4/PARAGRAPH/ADDRESSEE/SIGNATURE/DATE/REMARK）＋`AttachmentNode`＋`TableNode`；两个子类型改为继承共享基形状（不得 extends 联合）。运行时字段、字段名、type 取值零变化；`NodeType` 枚举 13 成员不动。受编译压力牵连的适配（parser.ts:306-307 一处窄化、5 个测试 makeNode 工厂各一行收窄断言、layout/index.ts:486/:492 冗余 cast 删除）随本条款一并完成。【验收：npm test 全量 331 用例绿＋npm run build（tsc -b）绿＋git diff 经技术负责人核对不含运行时逻辑变更（switch 仅加 case 标签/断言，快照文件零变更）】
- 条款2（0029·switch 穷尽断言）：为 4 处 NodeType switch 补齐显式 case 标签＋never 穷尽断言——layout/fonts.ts:33 `nodeFontRole`（补 ATTACHMENT/TABLE）、layout/index.ts:121 `paragraphAlignment`（补 8 个）、layout/index.ts:138 `paragraphIndent`（补 6 个）、hooks/useDetectionData.ts:145 `calculateBodyStats`（补 6 个）；断言写法须过 noUnusedLocals（如 `void (type satisfies never)` 或 types/ast.ts 内共享 assertNever），当前 default 分支返回值/行为一字不动。新增 NodeType 成员时 4 处须在 tsc 层报错。【验收：npm run build 绿＋npm test 全量绿（fonts.test.ts 既有 nodeFontRole 断言原样通过）＋人工步骤：临时给 NodeType 加一个假成员跑 tsc -b 应在 4 处报错后撤销（技术负责人亲验或抽验 diff 中 4 处断言形态）】
- 条款3（0029·切句类型表封闭）：utils/sentenceSplitter.ts:49-66 的 `SPLITTABLE_TYPES`/`NON_SPLITTABLE_TYPES` 类型标注由 `{ [key: string]: boolean }` 收紧为 `Record<NodeType, boolean>`（13 键已在、运行时对象与切句行为不动；切句逻辑与 sentenceId 拼接一行不改——红线）。【验收：npm run build 绿（新增 NodeType 成员时两张表 tsc 报错）＋npm test 全量绿】
- 条款4（0030·settings.xml 纳入断言网）：src/exporter/__tests__/docxBuilder.test.ts 的 `packParts`（:77-91）增加读取并返回 `word/settings.xml`；「奇偶页码（开）」用例加 `expect(parts.settings).toContain('<w:evenAndOddHeaders/>')`，「页码关」用例加等价负向断言（`toContain('<w:evenAndOddHeaders w:val="false"/>')` 或 `not.toContain('<w:evenAndOddHeaders/>')`）。不改生产代码、不改既有 13 处 toMatchSnapshot 基线、不给 settings.xml 新增快照。【验收：vitest 用例「奇偶页码（开）：奇数页码居右空一字，偶数页码居左空一字」「页码关：无页脚部件，正文无页码引用」绿＋__snapshots__ 目录 git 零变更】
- 条款5（收尾门禁）：两项完成后全量回归一次——npm test（14 文件 331 用例）与 npm run build 双绿，代码与档案同批提交。【验收：两条命令输出全绿，提交含 tasks/task-0002/ 档案更新】

## 明确不做

- 不改 AST 运行时结构：节点字段、type 取值、解析器识别行为、块序列输出全部不动（红线——0029 仅类型层）。
- 不动 sentenceId 生成与切句边界：sentenceSplitter 的切句逻辑、id 拼接格式一字不改（红线）；条款3 仅收紧两张表的类型标注。
- 不给 settings.xml 增加 toMatchSnapshot 快照：待办-0030 定位「一行断言级」，快照面不扩张（升快照须另裁）。
- 不收窄下游类型：LayoutParagraphBlock.sourceType、FontRole、DetectionPointType 等不在本任务封闭范围（如需另立待办）。
- 不改造 if/else 链式收窄为 switch：useDetectionData 标题检测、layout/index.ts runs 分支、renderContentFlow renderParagraphBlock 只关心子集，穷尽断言不适用，保持现状。
- 不修待办-0009（Toolbar hasContent 运行时缺陷）：联合封闭不产生该处编译错误，不搭车修（池内另案）。
- 不做 eslint 专项治理（待办-0024 已立项 task-0001）、不动 eslint 配置。

## 待澄清

- 无——夜班不可提问，两项全部以待办原文＋代码证据收敛定稿；0029 模式同构（DocumentConfigContext Action 为第一实例）已按规则在勘探.md「同构与复用判定」上报双选项，推荐就地实现，留技术负责人裁定，不阻塞条款实施（两选项对条款验收法无差异）。
