# 裁定｜task-0005 表格分页测量修复＋双轨瘦身评估

- 裁定人：技术负责人（夜班，用户不可及——按交接决策规则代拍板，醒后可复核推翻）
- 日期：2026-09-12

## 裁定1｜0005 采方案 A-简（DOM 同源测量），弃 A-行与方案 B

- **A-简（选定）**：measurer 模式渲染与 page 模式逐字节同构的真表格＋`usePagination` 选择器扩 `:scope > table`、表格整块一个 line。论据：与预览「DOM 度量分页」架构同向（测量即渲染，单一真值）；Phase 1 断点/版头扣除/Phase 2 版记避让零改动自动兼容；删除 mode 参数消亡双模式分叉。
- **A-行（否决）**：表格行级断点会切断表格边框（视觉缺陷引入）且分页逻辑复杂化。
- **方案 B 计算式（否决）**：与 DOM 度量架构逆行；宽度计量已有三版漂移风险（0016/0018 在池），再添一版计算式宽度是负收益。

**方案三问**：行为＝预览侧（含表格文档的分页断点位置纠正——缺陷修复）；导出零字节变化（exporter 零改动）；验证＝previewStructure 快照有意更新＋全量三绿＋人工步骤（表格跨页样例）；回滚＝git revert 单提交，无可逆面。

## 裁定2｜0032 拆半：IR 半截随本任务，AST 半截挂起待用户

- **IR 半截（随本任务，条款3）**：measurer 改真表格后 `LayoutTableBlock.rawContent` 零消费点——删字段＋供给＋测试构造。这是 0005 的自然尾巴（死代码即产即清），非独立行为变更。
- **AST 半截（挂起回池）**：`TableNode.content` 删除牵连 AI 送审行为三选项（a 不送审/b 重建文本/c 保留），是用户可感的 AI 校对覆盖面变化——夜班不可提问，不猜。挂起处置：0032 池行保持已立项指针但注记半截完成，AST 半截问题写清入池（新池行或注记）待用户裁定；与 0031 的交互（选项 a 使表格退出 sentenceId 域）一并注记。

## 裁定3｜同构判定采纳就地实现（不抽共用层）

版记 measurer（A4FooterNote）与本任务表格 measurer 为弱同构第二实例——但 A-简 方案使 measurer/page 双模式在表格上彻底同构后 **mode 参数本身消亡**（分叉点消失即最彻底的同构化），抽共用层无对象。巨型表格超单页保持现状截断（钉入明确不做）。

## 附记

- 勘探顺带发现 0042/0043/0044 已入池待分诊；0044（表格送审结果无处回贴）是裁定2 三选项的裁定输入，用户醒后一并看。
- 条款2 的三项人工验收步骤留用户；条款1/3/4 技术负责人机械亲验。
