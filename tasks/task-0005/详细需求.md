# 详细需求｜task-0005 表格分页测量修复＋双轨瘦身评估

> 定稿即冻结：后续改动走新裁定（裁定.md），不回改本件。
> 需求条款用列表行书写，每行必带【验收：…】——验收法写 vitest 用例名或人工步骤。
> 完整性由 `ctl check` 机械把关：离开澄清中前，条款行缺验收法、待澄清占位行残留、「明确不做」缺节，均为红。
> 勘探依据：tasks/task-0005/勘探.md（2026-09-12 夜班；修复方案采「方案 A-简」，0032 拆半见条款3 与「明确不做」）。

## 需求条款

- 条款1（度量容器表格渲染真实结构）：renderContentFlow 的 measurer 模式表格分支（renderContentFlow.tsx:471-475）改为渲染与 page 模式逐字节相同的结构化表格（`<table class="a4-table-element">`＋thead/headerCells＋tbody/dataRows），消除"表格按段落（Markdown 源文本）测高"的失真源头；measurer/page 双模式在表格上同构后，删除仅剩该处消费的 mode 参数（签名与 Preview.tsx:96 / A4Page.tsx:85 两处调用同步）【验收：previewStructure.test.tsx 的 Preview 系快照有意更新（度量容器内出现 a4-table-element 结构）＋npm test 全绿】
- 条款2（usePagination 表格测量分支）：行收集选择器由 `:scope > p` 扩为 `:scope > p, :scope > table`（usePagination.ts:165），table 元素按单行分支口径整块作为一个 line（offsetTop/offsetHeight，不可分割）参与 Phase 1 分页、首页版头扣除与 Phase 2 末页版记避让——分页断点不再落进表格内部【验收：人工步骤——粘贴含表格＋多段正文的样例公文（表格前后各有可跨页文本），预览中：a.表格后的内容不在表格前错页；b.断点落在表格边界外（表格不被拦腰切断或只显示残行）；c.末页开启版记时表格不与版记重叠】
- 条款3（0032-IR 半截：rawContent 随修复消亡后删除）：条款1 落地后 LayoutTableBlock.rawContent 无任何消费点，删除该字段（layout/types.ts:123-124）、其供给（layout/index.ts:345-347）与测试构造（styleFactory.test.ts:516）——TableNode.content 的其余消费点（sentenceSplitter AI 送审）不在本任务动，见「明确不做」【验收：npx tsc --noEmit 零 error（全键义务/联合封闭兜底）＋npm test 全绿】
- 条款4（测试网随工作增长）：表格解析行为若在实施中被触碰（本任务预期不触碰 parser），须先补 parser 表格用例再动；本轮至少保证 previewStructure 快照同步与全量门禁（npm test＋npm run build）一次绿收尾【验收：npm test 全绿＋npm run build 成功】

## 明确不做

- docx 导出产物字节不变：导出侧不消费 rawContent（勘探 §3.2 #5），本轮对 exporter/ 零改动（docxBuilder.test.ts 13 条导出快照为字节不变证据）。
- sentenceId 与切句边界不动（红线）：本轮不动 sentenceSplitter/aiHighlight/textBlockSplitter 的任何行为。
- 0032-AST 半截挂起：TableNode.content 字段删除与 sentenceSplitter 表格送审行为裁定（a 不送审 / b 重建文本 / c 保留）不随本任务做——AI 校对行为属用户可感知变化，夜班不可提问，挂起待用户醒后裁定（三选项数据见勘探 §七-2；裁定后可另立单元或新待办）。
- 表格跨页视觉重排不做：断点正确后表格仍为整块放置；巨型表格（高于一页可用高）保持现状"推到新页起＋viewport 截断底部"（与单行段落超高行为一致）；"按表格行跨页＋每页重渲染子表"属渲染机制升级，须另立任务。
- 页面表格上下间距（.a4-table-element 无 margin 的视觉贴边现状）不动：两侧（预览/导出）现状均无显式表格前后距，非本任务偏差族。
- 度量容器版记机制（A4FooterNote measurer）不动：仅表格分支改动。

## 待澄清

- 无（夜班约束不可提问：唯一未决项＝0032-AST 送审行为三选项，已按纪律挂起至「明确不做」第 3 条并落档勘探 §七-2 交技术负责人组织裁定，不以猜测入条款）。
