# 复核｜task-0001 单元6（配置双轨合并）

> 复核员（提示词 05，技术负责人派出）；2026-09-12。
> 被复核单元＝单元6；diff 范围＝提交 c1efe51（代码）＋3edfd12（档案，仅待办池 +3 行）。
> 方法＝独立第二读：对照裁定 §三（用户已追认）与详细需求条款6 通读全部 diff、逐条验测试网真伪、亲跑验收命令。

## 一行结论

**通过**——六个复核重点逐项核实无行为偏离、无测试欠账、无结构偏离；附 1 项任务级机械面红灯与 3 项非阻断观察（均不属单元6 交付物缺口）。

## 机械面（开工首跑）

`ctl task check task-0001`：**FAIL，1 项违规**——`工作单元-7.md 附录未填:（范围、边界、交付物）`。
定位：tasks/task-0001/工作单元-7.md 第 47-49 行残留派工模板的空占位行（`2. **一句话范围**：`／`3. **边界（不做）**：`／`4. **交付物**…：`），与上方已填实的同编号条目重复，机械检查据空行判红。
归属：本复核单元自身的派工档案结构欠账（技术负责人派工时残留），非单元6 交付物缺陷。下一步＝技术负责人删除这 3 行空占位即绿。

## 逐项核查记录（附录指定的 ①~⑥）

### ① 迁移特征检测的误判/漏判边界 —— 通过

证据＝src/types/documentConfig.ts `needsMigration`/`migrateDocumentConfig`/`loadMigratedConfigStorage`（diff 亲读）＋ documentConfigMigration.test.ts。

- **二次启动幂等**：迁移产物无 `advanced` 键、headings 四元素各含 `asciiFontFamily` → `needsMigration` 返回 false → 不再备份不叠加回写。专测「幂等：对已迁移主键再次调用不产生新备份、主键内容不变」（二次调用后主键字符串严格相等、备份键数不变）——真断言非空转。
- **纯新版结构**：`needsMigration` 三重特征（无 advanced＋四元素齐＋各含 asciiFontFamily）判 false，直接读取。专测「新结构存储不迁移（migrated=false，savedConfigs 逐项引用原样）」＋「新结构主键：直接读取，无备份产生」。
- **headings 缺字段的半新/更旧结构**：`'advanced' in record` 或 headings 缺任一必需键即判旧——宁迁勿漏；迁移本身无损（`{...DEFAULT_CONFIG, ...record}` spread 保留其余字段）。专测「advanced 缺席的更旧结构：h1/h2 取 headings 值，asciiFontFamily 回退默认，h3/addressee 取默认」。
- 理论边缘（不构成偏离）：部分破损的新结构（如仅 h2 缺 asciiFontFamily）触发整档迁移后，其余元素已有的 asciiFontFamily 会重置为默认——mergeElement 只从 advanced 读 ascii。现实存储仅由应用写全结构，不会出现此形态；且函数既定口径「脏输入回退默认」。记录备查。
- 误判方向反向核查：新结构各元素 `asciiFontFamily` 恒为 string（含空串＝「跟随中文字体」，`typeof === 'string'` 判定保留空串，专测覆盖）→ 不会把合法新结构误判为旧。

### ② 备份键写入与回滚演练 —— 通过

- **原样逐字节**：`loadMigratedConfigStorage` 备份的是 getItem 返回的原始字符串（`store.setItem(prefix + Date.now(), raw)`），不经 parse/stringify 往返。专测 `expect(store.data[backupKeys[0]]).toBe(raw)`——严格相等。
- **时间戳**：键名＝前缀＋`Date.now()`，正则断言 `^docx-document-config-v2\.backup-\d+$`。
- **回滚演练路径真实性**：专测真执行「清空主键→找备份键→恢复其内容到主键」，并断言恢复物＝旧结构原貌（`advanced` 在、`headings.h1 = {fontFamily:'宋体', fontSize:22}` 旧形态可读）——不是注释里的空承诺。
- 主键就地升级＋立即回写（幂等的前提）；主键名 `docx-document-config-v2` 前后不变（裁定 §三 字面要求）。

### ③ 多档 savedConfigs 逐档迁移 —— 通过

专测「多档 savedConfigs 各自迁移」：3 档（冲突档/默认等值档/已新结构档）混合输入——冲突档 h1 取 advanced（黑体）、等值档产出完整新形态、新结构档 `toEqual(DEFAULT_CONFIG)` 原样；`activeConfigId`/`aiProofreadConfig`（哨兵对象）与档名/createdAt 元数据逐项保留。`migrated` 标志只在确有旧档时为 true。

### ④ 「-2 条旧断言翻转」等效性 —— 通过（等效保留，非覆盖洞）

开工前测试网中的双轨特征对（git grep c1efe51~1 亲核）：
- fonts.test.ts：删「改 config.headings.h1 不影响决策层字体」＋「改 config.advanced.h1 影响决策层字体」，翻转为 1 条「改 config.headings.h1 影响决策层字体（单元6 单一真值）」（fontFamily 宋体＋sizeHalfPt 44 双断言）；
- styleFactory.test.ts：同构的 2 删 1 翻转（导出侧同款正向断言）。

裁定 §三 使「headings 轨无效」断言的语义被有意废止——旧对的另一半（真值轨道驱动渲染）以正向断言在两文件各自保留；ascii 空串回退/显式指定两侧行为断言原样改读到新路径。净 -2 与提交说明一致，数字对账：312（单元5 收尾）＋16 新增−2＝326。

### ⑤ SettingsModal 两区度止后无残留 —— 通过

grep 亲核（全 src）：
- `advanced` 运行时引用仅剩 documentConfig.ts 迁移代码自身（Legacy 视角类型/`'advanced' in record` 检测/迁移后 delete——合法必需）与两处历史注释；`AdvancedConfig`/`AdvancedElementConfig` 类型零残留；
- `showAdvanced`：零命中（state 与折叠区块整段删除）；
- `settings-advanced*`、`settings-section-toggle`、`settings-arrow`：tsx 与 css 双零命中（折叠按钮死样式同步删除）；
- 新结构核对：单一「标题字体」区＝h1/h2/h3/addressee ×（中文字体/英数字体/字号）三控件组，`settings-grid--3` 存在（SettingsModal.css:110），patch 深合并语义与旧 advanced 区同机制。新 CSS 仅改名无 Chrome 78 违禁特性（无 gap/inset/:has/clamp）；新增代码无 `?.`/`??`/`replaceAll`/`.at()`。

### ⑥ 快照零 diff 真伪 —— 通过（快照从未被碰）

- `git diff c1efe51~1 c1efe51 -- "**/__snapshots__/"` ＝ 空（0 行）；扩展到 `c1efe51~1..HEAD` 同空；
- 两提交的 `--stat` 文件清单均不含任何 `__snapshots__/` 文件 → 不是「改了快照再改回」，快照未经触碰；
- 在盘核对：docxBuilder.test.ts.snap ＝ 13 条、previewStructure.test.tsx.snap ＝ 14 条（`grep -c "^exports\["`），与基准口径一致；buildLayout 3 条（前序单元既有）亦未动。
- 有效性佐证：DEFAULT_CONFIG 两轨默认值等值有专测锁定（「默认值与旧两轨默认等值」），快照输入走默认配置 → 零 diff 是行为保持的结果而非快照失能。

## 对照裁定 §三 与条款6 验收法

| 要求 | 裁定/条款出处 | 核实 |
|------|--------------|------|
| 单一「标题字体」区，每级中文字体/英文字体/字号三控件 | §三/条款6 | ✓ SettingsModal 区块3 重构 |
| 度止基础区/高级区 h1/h2 重复入口 | §三 | ✓ 两区整段删除（diff 亲读） |
| addressee/h3 扩展字段并入同区 | §三 | ✓ 同区第 1/3 行 |
| headings/advanced 收敛单一真值，类型/DEFAULT_CONFIG/预览/导出同步 | 条款6 | ✓ AdvancedConfig 删除；消费点收敛清单＝layout/fonts.ts roleSpec 四角色、Preview.tsx `--h3-font`、layout/types.ts 注释（c1efe51 提交说明逐处列明，diff 逐一核实） |
| 键 `docx-document-config-v2` 不变，读时自动升级 | §三 | ✓ CONFIG_STORAGE_KEY 常量，loadStorage 改经 loadMigratedConfigStorage |
| 两轨冲突以 advanced 为准 | §三 | ✓ mergeElement 逐字段 adv→headings→默认；专测 |
| 升级前原样备份 `…backup-<时间戳>` | §三 | ✓ 见② |
| 用户不丢失任何已保存配置 | 条款6 | ✓ 备份键＋多档迁移＋元数据保留 |
| vitest「旧→备份→迁移→新」断言 | 条款6 验收法 | ✓ documentConfigMigration.test.ts 16 例（无 skip、无空断言、内存替身注入真副作用） |
| 人工步骤：旧版配置启动确认迁移后预览/导出一致 | 条款6 验收法 | **未留痕**（见观察 B） |

行为改变面核查：唯一行为变化＝「两轨冲突的历史自定义配置」合并后单侧跟随 advanced（裁定明示放行）；默认路径零变化（两轨默认等值锁定＋27 条快照零 diff 佐证）。偏差开关默认值未动（deviations.ts 不在 diff 文件清单）；A4Page.css/预览 DOM 结构未动（不在清单）；sanitize/parser/AST 未动（不在清单）；无新依赖（package.json 不在清单）。

## 测试网真伪（网随工作增长）

- 新增 documentConfigMigration.test.ts 16 例，全部实质断言（备份逐字节 `toBe(raw)`、正则键名、引用恒等 `toBe(freshSaved[0])`、多档哨兵），零 skip 零 todo；
- 触碰面（标题字体真值消费）由旧 4 条双轨特征断言翻转为 2 条单一真值正向断言＋2 条 ascii 边界断言（改路径保留）——旧「headings 无效」断言的废止正是本单元的行为变更本身，语义等效；
- 全量 312→326，净增 14，全部落在被触碰行为面上。

## 亲跑验收命令输出摘要

| 命令 | 结果 |
|------|------|
| `npm test` | ✓ 13 文件 / **326 passed (326)**，0 failed 0 skipped；documentConfigMigration 16、fonts 16、styleFactory 60、docxBuilder 10（13 快照）、previewStructure 14 |
| `npm run build` | ✓ built in 2.13s（PWA precache 29 entries 正常产出） |
| `SINGLE_FILE=1 npx vite build` | ✓ built in 2.14s，dist/index.html 1,238.61 kB 内联产物 |
| `ctl task check task-0001` | FAIL 1 项（工作单元-7.md 空占位行，见机械面节，非单元6 缺陷） |
| `git diff c1efe51~1 c1efe51 -- "**/__snapshots__/"` | 空（0 行） |

## 逻辑同构（平行实现拦截）

- 主键 `docx-document-config-v2` 全库单一读写路径（DocumentConfigContext 经 loadMigratedConfigStorage），无第二处 parse/merge 逻辑；
- mergeElement 的 adv→head→默认回退链与 Context 的 deepMerge（patch 合并）目的不同形不同，非平行实现；
- 工程师顺带发现已按纪律入池不改码：待办-0025（预览 h3 字号未接线）、0026（预览主送字体/字号未接线）、0027（SettingsModal.css 重复规则块）——3edfd12 仅动待办池.md，代码零改动，核实属实。

## 观察项（非阻断，交技术负责人分诊）

- **A（结构欠账）**：工作单元-7.md 空占位行致 ctl check 红（见机械面节）——删 3 行即绿，不影响本复核结论。
- **B（人工步骤未留痕）**：条款6 验收法的人工步骤（旧版保存配置启动、迁移前后预览/导出一致确认）与单元6 交付物「人工走查项」在档案中无执行留痕。与单元5 先例一致（人工走查转交付阶段），建议验收回填时显式补做或声明转交付阶段。
- **C（极窄错误路径差异）**：旧 loadStorage 的 try 包住了 `localStorage.getItem`；新 loadMigratedConfigStorage 中 getItem 在 try 外——仅存储被彻底禁用的极端环境抛错会传到启动路径，支持的 Chrome 78（http/file://）不可复现。不构成对裁定的偏离，记录备查；若技术负责人要求，补一层 try 为三行改动。
- 另注：SettingsModal 新提示语「同时作用于预览与导出」在 h3 字号与主送机关两行上因 0025/0026（预览侧未接线、条款9 冻结）暂不完整成立——文案与冻结缺陷的取舍交技术负责人（池中已有对应行）。

## 文档更新清单（通过结论下的改齐，同批提交）

- AGENTS.md:133 `headings: HeadingsConfig // 各级标题字体` → `// 标题与主送机关字体（单一真值，含中文字体/英数字体/字号）`——单元6 使该注释失真（headings 现含主送机关），逐行改齐；AGENTS.md 全面重写仍属单元8（条款8）。
- 其余核查：AGENTS.md/README.md 无 `advanced`/「高级设置」/双轨失真引用；库无 docs/ 目录；无需更多改动。

## 给技术负责人的下一步

1. 验收回填单元6（本复核结论＝通过）；
2. 删工作单元-7.md 第 47-49 行空占位，消除 ctl check 红；
3. 分诊观察 B（人工确认补做或转交付阶段）与观察 C（是否补 getItem 容错）；0025/0026/0027 按池流程待议。
