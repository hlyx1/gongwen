# 详细需求｜task-0001 可维护性架构重构

> 定稿即冻结：后续改动走新裁定（裁定.md），不回改本件。
> 需求条款用列表行书写，每行必带【验收：…】——验收法写 vitest 用例名或人工步骤。
> 完整性由 `ctl check` 机械把关：离开澄清中前，条款行缺验收法、待澄清占位行残留、「明确不做」缺节，均为红。

## 需求条款

- 条款1（测试安全网先行）：在任何业务代码改动之前，为现有行为建立特征测试锚点——a) `buildDocument(ast, config)` 导出产物结构断言/快照（至少覆盖：全节点类型样例公文、版头开/关、版记开/关、奇偶页码、印章有无、单/多附件、含表格）；b) `sanitizeText` 清洗规则用例补齐（现无专项测试文件）；c) 决策素材函数行为记录（签名缩进计算、charSpacing 计算，作为决策层等价性对照）【验收：vitest 用例 exporter/__tests__/docxBuilder.test.ts、utils/__tests__/sanitize.test.ts 全绿；导出快照＝docx 库 Packer 序列化产物核心部件（word/document.xml 等）结构快照，快照文件入库】
- 条款2（排版决策层）：新建纯函数决策模块（建议 src/layout/，命名由技术负责人定），输入 `GongwenAST + DocumentConfig`，输出渲染器无关的版式决策中间表示（块序列：块类型/对齐/缩进/空行指令/字体角色分段 runs/表格结构/版头版记页码版式参数）；决策层不含任何 docx 类型或 React/DOM 依赖【验收：vitest 用例 layout/__tests__/*.test.ts 对每个 NodeType 的决策输出做快照断言；tsc 无 docx/react 类型渗入该模块（人工步骤：检查 import 列表）】
- 条款3（docx 渲染器退化）：docxBuilder/styleFactory 退化为「决策中间表示 → docx 对象」翻译层；重构后对条款1 同样的 AST+Config 输入，导出产物与重构前快照一致。已知偏差按裁定 §一（Q1＝方案A：统一真值＋显式偏差开关）执行：五项已知预览/导出偏差（勘探.md §3.6，对应待办-0001~0005）各设一个显式偏差开关，集中定义于决策层一处，默认值＝现状行为，决策层按开关产出各渲染器的最终决策；重构完成后偏差修复＝晋升对应待办并翻转开关，偏差从隐性散落变显性集中可审计【验收：vitest 用例「重构前后导出快照一致」——docx 库 Packer 序列化产物核心部件（word/document.xml 等）结构快照逐字段一致；决策层快照对五项偏差各断言两渲染器现状输出不变】
- 条款4（预览渲染器退化+A4Page 拆解）：A4Page 退化为决策层输出的渲染器，并拆出 a) AI 高亮包装层（合并 4 个重复的 renderHeadingXWithHighlight 为 1 个参数化实现）；b) 版头/版记/页码子组件；预览 DOM 类名与结构保持不变（A4Page.css 不动）【验收：预览 DOM 结构与 className 断言＋分页边界（总页数、每页首行内容）一致；人工视觉走查兜底——对同一样例公文逐页截图对比重构前后预览（含版头、版记、多附件、表格、AI 高亮悬停）】
- 条款5（度量容器同源化）：Preview.tsx 度量容器与 A4Page 消费同一渲染输出（消灭第三次节点遍历复制）；保持度量容器 DOM 类名结构（usePagination.ts:238 `:scope > p` 选择器依赖），表格分支缺失现状原样保留（待办-0005 冻结）【验收：分页边界（总页数、每页首行内容）一致；人工视觉走查兜底——含签名/备注空行、末页版记避让场景逐页确认】
- 条款6（配置双轨合并）：按裁定 §三（Q3＝方案C，初裁待用户追认，实施前可改判——否决则按新裁定改判，影响面仅配置合并单元）——SettingsModal 重构为单一「标题字体」区（每级标题一组：中文字体/英数字体/字号），废止基础区/高级区中 h1/h2 的重复入口，advanced 的 addressee/h3 扩展字段并入同区；`config.headings.h1/h2` 与 `config.advanced.h1/h2` 收敛为单一真值，DocumentConfig 类型、DEFAULT_CONFIG、预览与导出消费点同步收敛；已存 localStorage（`docx-document-config-v2`）读时自动升级，两轨值冲突以 advanced 为准（字段最全、驱动导出），升级前旧结构原样备份至 `docx-document-config-v2.backup-<时间戳>` 键（回滚＝清空新键、恢复备份键），用户不丢失任何已保存配置【验收：vitest 用例「旧版存储结构→备份→迁移→新版结构」断言（含两轨冲突以 advanced 为准、备份键写入）；人工步骤——用旧版保存的配置启动应用，确认字体设置与迁移前预览/导出效果一致】
- 条款7（清理治理）：a) 移除 usePagination.ts（48 处）、App.tsx AI 配置打印（15 处）的调试 console.log；aiProofreadService 21 处运行日志按裁定 §四（Q4＝方案A）全部移除（含完成后向控制台倾倒原始响应），statsPrinter 刻意统计打印保留；b) 删除死代码 constants/gongwen.ts 与 LINES_PER_PAGE 常量；c) 删除 DocumentConfigContext 的 aiProofreadConfig 死路径（App 实际走独立 state+键）【验收：人工步骤——`grep -rc console.log src` 仅剩 statsPrinter；`grep -rn constants/gongwen src` 无结果；构建与全测试绿】
- 条款8（AGENTS.md 同步重写）：按重构后架构重写——目录结构、数据流（含排版决策层）、配置结构（合并后）、删除「虚拟滚动」等失实描述、源码 JS 语法放宽说明（esbuild chrome78 保证产物兼容，CSS 限制保留）【验收：人工步骤——技术负责人对照实际代码抽查目录树与数据流描述逐项属实】
- 条款9（已知不一致冻结）：待办-0001~0008 所列问题在本次重构期间一律不修、不顺带改；若架构落地使某偏差自然显式化，仅登记于裁定.md 不改变行为【验收：人工步骤——对照待办-0001~0005 逐项在重构前后导出 docx 与预览中复现确认行为未变】

## 明确不做

- 不修任何预览/导出已知不一致（待办-0001~0005）及新入池顺带发现（Toolbar hasContent、切句不一致等）——只登记
- 不改变 sanitize 与 parser 的解析/清洗行为（它们是行为锚点）
- 不改变 AST 结构与 sentenceId 生成规则（AI 校对链路依赖）
- 不改变预览视觉输出（CSS 视觉效果保持）
- 不引入 dsh-tool-hongtou 的任何代码、模板、依赖（只借鉴架构思想）
- 不实现 AI 起草/AI 管线降级（待办-0006/0008，仅为其留决策层接口位）
- 不改构建目标与部署方式（chrome78、GitHub Pages/Vercel/单文件离线不变）
- 不做移动端适配、不加新功能

## 待澄清

> 四问已于 2026-09-11 裁定回填（Q1→条款3、Q2→条款1/3/4/5 验收口径、Q3→条款6、Q4→条款7），详见 裁定.md §一~§四；本节无未决问题。
