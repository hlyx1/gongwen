# 详细需求｜task-0007 切句对齐：高亮侧复用送审切句

> 定稿即冻结：后续改动走新裁定（裁定.md），不回改本件。
> 需求条款用列表行书写，每行必带【验收：…】——验收法写测试用例名或人工步骤。
> 完整性由 `ctl check` 机械把关：离开澄清中前，条款行缺验收法、待澄清占位行残留、「明确不做」缺节，均为红。

## 需求条款

- 条款1（切句同源：复用送审切句）高亮侧切句改为 import 复用 `utils/sentenceSplitter.ts` 的单节点切句函数（推荐形态：`splitNodeIntoSentences` 新增 export，零逻辑改动——落点双选项及证据见勘探.md「同构与复用判定」，由技术负责人裁定）；`aiHighlight.tsx` 的 `splitHighlightSentences` 删除或退化为对 sentenceSplitter 的转发，冻结注释同步改写为「切句以 sentenceSplitter 为单一真值」。正文族（PARAGRAPH/ADDRESSEE）句集合、边界、trim 口径与送审侧逐字符一致——差异清单（勘探 §二）1~7 号边界全部一致化：双字省略号不再碎片、单字『…』不再切、配对符号（含未闭合与「首现即弃」现状）内分隔符不再切高亮。【验收：新增对照测试（建议 src/components/Preview/__tests__/aiHighlight.test.ts，或随待办-0043 建 src/utils/__tests__/sentenceSplitter.test.ts 并互引）——对差异样例集（双字省略号中置/接句号、单字省略号中置、书名号内句号、括号内句号首现受保护、未闭合括号、引号内句号、连续句号、句间空白）断言高亮侧切句输出与 splitNodeIntoSentences 逐句一致；npm test 全绿】
- 条款2（标题体 seq 连续＋首句边界同源）`renderHeadingRuns`（renderContentFlow.tsx:289-354）首句聚合边界从 `indexOf('。')` 改为送审切句的首句边界（？！；……同入边界），剩余部分句序号自 2 起连续（与送审 localSeq 对齐）——消灭「剩余 seq 自 1 重起算」的 id 碰撞（首句与剩余首句同 id 双高亮）与剩余第 k 句错位（高亮 k vs 送审 k+1）。首 run 类名方案（HEADING_FIRST_INLINE_CLASS 按段落源类型）与剩余部分 a4-paragraph-inline 宿主包装结构保持，仅句边界与序号来源换轨。【验收：previewStructure.test.tsx「AI 校对结果：标题/正文句子/一级标题首句高亮」快照有意更新——一级标题剩余句 span 失去 a4-highlight-sentence（makeAiResults 无 HEADING_1-6-2，碰撞消除）；新增用例「一级标题第二句以 seq=2 命中高亮」「标题含『？』时首句边界止于『？』不再罩全段」；npm test 全绿】
- 条款3（整节点族对齐）SIGNATURE/DATE/REMARK 的高亮查询归整节点 seq=1（镜像送审 `NON_SPLITTABLE_TYPES` 门，表已导出），不再切句分配 seq——REMARK 含『；』等分隔符时整段单句命中罩全段（消灭「只罩前半」）。run 级标点 span（时间冒号）渲染结构保持，仅句序号来源换轨。【验收：新增用例「备注含分号：整段以 REMARK-<line>-1 单句命中、高亮罩全段」（现状仅罩『；』前半）；npm test 全绿】
- 条款4（多段标题 id 对齐）DOCUMENT_TITLE 各标题块统一查询送审合并句 id `DOCUMENT_TITLE-<首个标题块 sourceLineNumber>-1`（行号自 blocks 序列推导，渲染器内可算，layout 零改动）；单段标题行为不变。【验收：新增用例「两行标题：第二行标题块以 DOCUMENT_TITLE-1-1 命中并获得 a4-highlight-sentence」；previewStructure 既有两行标题样例快照相应更新（现状第二行无高亮）】
- 条款5（冻结面同步登记）sentenceId 格式（type-line-seq）与 lineNumber 键语义不变；送审侧 sentenceSplitter 切句行为一字不动（红线：仅允许新增 export 级改动）；aiHighlight.tsx 文件头冻结注释改写，登记「0013 对齐后高亮侧切句以 sentenceSplitter 为单一真值，改切句行为须双侧同变」。【验收：人工——git diff 确认 sentenceSplitter.ts 除 export 外零变更；grep src 确认旧冻结字样「正则与旧实现逐字符一致」无残留】
- 条款6（基线同批与门禁收尾）previewStructure.test.tsx 2 条 AI 高亮快照（:235-241、:276-285）随代码同批有意更新，工作单元实施记录附变更前后差异说明；全量门禁收尾一次绿。【验收：npm test＋npm run lint＋npm run build 三绿；快照 diff 与代码同批提交且实施记录含差异说明】

## 明确不做

- sentenceSplitter 切句逻辑一字不动（红线）：不改 DELIMITERS/配对符号表/「首现即弃」现状/表格整块送审（:310-327）——首现即弃已入池待议，本任务高亮侧照单继承
- 送审管线不动：useAIProofread/textBlockSplitter/aiProofreadService/aiResponseParser 零变更
- ATTACHMENT/TABLE 不新增高亮展示：送审有句、高亮无展示属「无回贴」族（待办-0044），本任务不扩显示面
- 0017/0046 联动不做：连续句点方向 B 不预裁、单字『…』送审侧语义不改（对齐后高亮碎片消灭即 0046 高亮侧面向量消失，池备注改写归分诊）
- layout/、exporter/、parser/、sanitize 零改动；sentenceId 格式与 lineNumber 键语义不动（待办-0031 挂起维持）
- 不做切句 memo 化性能优化（勘探 §五微基准：微秒级，无必要）

## 待澄清

- 无占位残留。夜班约束（用户不可问）下依用户红线预裁收敛：「高亮侧对齐送审切句边界（复用而非重写）」已定方向，条款1~4 均为该方向的行为展开；唯一实现形态分叉（新增 export 单节点函数 vs 抽共用层）已在勘探.md「同构与复用判定」并列双选项上报技术负责人裁定，不作为待澄清挂网。
