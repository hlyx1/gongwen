# 工作单元-2｜task-0007 切句对齐：高亮侧复用送审切句（实施）

## 底座＝角色提示词原文

> 由 `ctl dispatch` 自动拼接（prompts/03-工程师.md），以下为派工时的最终全文留档——重启与验收可回查当时派的到底是什么。

# 提示词 03｜工程师（技术负责人派出）

> 用法：技术负责人在契约定稿后派出，可多次派出（一项任务通常多个实施单元）。

你是**工程师**，按工作单元实施。你是在为**结构质量**工作，不是在堆功能——产出以结构质量＋行为保持衡量，不以"功能跑起来"衡量。

## 你要做的

1. **读工作单元六要素**：范围、边界、必读文件、交付物——清单外事项一律不做
2. **测试先行——无锁定，不动手**：要触碰的每一块行为，动手前先有测试锁住它。重构类：对着现状写特征测试、看着它绿再动、全程保持绿；新功能类：**详细需求的条款与验收法就是考卷**——先写测试（红）再实现到绿。出题权在外部，你不给自己的实现出题
3. **小步提交，每步回路绿**——回路＝只跑本次触碰面直接相关的测试＋项目最快的静态检查；**全量（全测试＋构建门禁）收尾一次**，交卷必须全量绿。代码与档案同批提交——档案含受影响的 Agent 文档（AGENTS.md 等）与文档集群，改到与代码一致
4. **顺带发现**：范围外的问题、可疑缺陷，一行一条记入"顺带发现"——只上报不顺手修，并 `ctl backlog add "…" --source 工程师 --status 待议` 逐条入池（池是唯一入口，01 按状态分诊）。**特别盯逻辑同构**：你正在写的东西若与库中某处逻辑形状相同（同一类资源、同一个动作、同一条链路——哪怕没有一行代码相同），这就是上报信号：交技术负责人开裁定（就地实现 vs 抽共用层），不擅自改道、不就地扩界
5. **收尾**：跑工作单元指定的验收命令；对照交付物清单逐件自查在盘；`ctl task check <task>` 全绿再交卷——你也动了档案（工作单元尾部、Agent 文档），别把结构红灯交给技术负责人；向技术负责人汇报一行＋证据路径

## 纪律

- 发现实际范围超出工作单元所述 → 停下上报升级，不自行扩权
- **测试网随工作增长**：单元结束时，被覆盖的行为面只能比开工时更大
- 确实无法自动验证的面（如纯视觉），显式声明并给出人工核验步骤，不得静默跳过


## 附录·六要素

1. **必读文件**：
   - tasks/task-0007/详细需求.md（需求条款＝考卷,验收法即判卷标准）
   - tasks/task-0007/勘探.md（关键文件地图——省探查时间）
   - tasks/task-0007/裁定.md（选定方案与被否理由——照裁定实施,不擅自改道）
   - 代码库文件（技术负责人补入——取自勘探文件地图与裁定落点）:
2. **一句话范围**：按详细需求条款1~6 实施（裁定1 形态 A）：sentenceSplitter 新增 export（零逻辑改动）＋高亮侧切句换轨（splitHighlightSentences 删除/转发）＋条款2 标题体 seq 连续与首句边界同源＋条款3 整节点族 seq=1＋条款4 多段标题合并 id＋条款5 冻结注释改写＋条款6 快照同批与门禁。
3. **边界（不做）**：详细需求「明确不做」7 项——sentenceSplitter 切句逻辑一字不动（仅 export）、送审管线零变更、layout/exporter/parser/sanitize 零改动、sentenceId 格式与 lineNumber 键语义不动、ATTACHMENT/TABLE 不新增高亮、0017/0046 联动不做、不 memo 化。
4. **交付物**（逐件列出——验收时对照在盘逐件核）：①条款1~5 代码变更（含对照测试新增——差异样例集八类边界断言高亮侧与 splitNodeIntoSentences 逐句一致＋条款2/3/4 各自新用例）；②previewStructure 2 条 AI 高亮快照有意更新（与代码同批）；③npm test＋npm run lint＋npm run build 三绿；④变更前后差异说明（高亮罩句修复面——八类边界＋四项结构性错位）写入实施记录；⑤工作单元尾部实施记录与提交哈希。
5. **验收依据**：回路绿逐步验（触碰面相关测试），全量（全测试＋构建门禁）收尾一次绿；网随工作增长（触碰的行为面有测试）；留痕（工作单元在档、同批提交）
6. **身份提醒**：你是为结构质量工作的，不是为堆功能工作的；产出以结构质量＋行为保持衡量。**夜班特别约束：用户在睡觉、不可提问——拿不准照裁定与详细需求自决，超边界发现只入池不搭车修。测试先行：对照测试先红后绿；sentenceSplitter 的 git diff 除 export 行外必须为零（条款5 验收人工步骤，技术负责人会亲核）。**

## 验收结论（技术负责人回填）

（通过／退回及理由；日期）

---

## 实施记录（工程师回填，2026-09-12）

### 提交清单

- `f4831de` 档案: task-0007 立项派工入库——开工时任务 6 件与待办池改动（0013 立项指针＋0048/0049）尚在工作树未入库，先补立项提交再动代码（镜像 task-0006 之 6f00c46）
- `928eb93` 修复(0007,裁定1形态A): 代码＋对照/DOM 测试＋2 条快照有意更新＋AGENTS.md 三处同步（6 文件，+385/−83）
- 本提交＝档案回填（实施记录＋任务概况单元2 置完成）

### 红→绿记录（测试先行）

1. **DOM 用例先行（红 6 / 绿 1）**：新增 `src/components/Preview/__tests__/aiHighlight.test.tsx` 的 7 条 DOM 用例（renderToStaticMarkup＋buildLayout 真实链路），对旧实现跑出 6 红：双字省略号碎片（`未尽事宜另行通知…`＋裸 `…` span）、书名号内句号切高亮、一级标题第二句 seq=2 罩不住、`？`首句罩全段、REMARK 含`；`只罩前半、两行标题第二行 miss；绿 1＝单段标题行为不变（保持用例）。
2. **对照测试补入（红 27 / 绿 1）**：补差异样例集八类边界对照（sentencesForBlock vs splitNodeIntoSentences 逐句一致＋送审基线锁定）＋主送/标题体/整节点族对照共 21 例——引用尚不存在的新 export，文件级导入失败红。
3. **实现转绿（28/28）**：sentenceSplitter 增 export（1 行）→ aiHighlight 收敛 → renderContentFlow 换轨。**中途一次返工**：renderSegmentsWithHighlight 区间映射 `fullText.indexOf(sentence)` 未随句子对象化改为 `sentence.text`，片段渲染为空——DOM 用例当场抓住（高亮 span 空文本），修正一行后全绿。

### 快照变更统计（previewStructure.test.tsx.snap）

2 条目 / 2 行：`AI 校对结果：标题/正文句子/一级标题首句高亮`＋`加盖印章·含 AI 高亮：度量容器无高亮、页面有高亮`。语义变更各 3 处（A4Page 用例 1＋Preview 用例双页 2）：

- 第二行标题**获得** `a4-highlight-sentence`（DOCUMENT_TITLE-1-1 合并 id 命中——条款4）
- H1 剩余句**失去** `a4-highlight-sentence`（HEADING_1-6-2 不在 makeAiResults——id 碰撞消除，条款2）
- 度量容器标题保持无高亮（measurer 段零变化）；其余 14 条快照（含全部无 AI 用例）逐字节零变化

### 变更前后差异说明（高亮罩句修复面）

- **正文族（PARAGRAPH/ADDRESSEE）八类边界一致化**：①双字`……`不再拆两碎片句（命中罩整对）②`……。`不再碎片＋裸句号错位 ③单字`…`不再切（送审语义）④书名号内句号不切 ⑤括号内句号首现受保护（**首现即弃现状照单继承**——0048：剩余文本并作一句）⑥未闭合括号全文一句 ⑦引号内句号不切 ⑧连续句号/句间空白两侧本一致（锁定为基线）。
- **标题体（一至四级）**：首句边界从 `indexOf('。')` 换为送审首句边界（`？！；……`入界）——`一、完成任务？后续。`旧罩全段→新止于`？`；剩余句 seq 自 2 连续——消灭「首句与剩余首句同 id 双高亮」与「剩余第 k 句查 id=k、送审为 k+1 的罩不住」。跨界 run 就地拆两段（同规格），首 run 类名方案与剩余 a4-paragraph-inline 宿主包装结构保持。
- **整节点族（SIGNATURE/DATE/REMARK）**：整段单句 seq=1（镜像 NON_SPLITTABLE 门）——REMARK 含`；`命中罩全段（旧只罩前半）；run 级标点 span（时间冒号）结构保持。
- **多段标题**：第二行起统一查 `DOCUMENT_TITLE-<首标题块源行号>-1`（行号自 blocks 序列推导，layout 零改动）；单段标题行为不变（有保持用例）。
- **行为可见面**：仅「AI 结果 Map 非空且命中上述节点族」的文档受影响；无 AI 结果时退化为纯文本路径不变（14 条无 AI 快照零变化佐证）。
- **附带语义（条款2 预定行为面，非缺陷）**：标题首句以`？！；……`结束且后随文本时，预览侧其后文本落 a4-paragraph-inline（正文字体类）——layout run 分段仍只认`。`（零改动边界），导出侧该文本仍随标题字体；此为「剩余部分 a4-paragraph-inline 宿主包装结构保持」的直接后果，无 AI 结果时同样成立（既有快照样例标题全以`。`收尾故不涉）。

### 红线自查（条款5 人工步骤，供技术负责人亲核）

- `git diff 928eb93^ 928eb93 -- src/utils/sentenceSplitter.ts` ＝ 仅 export 块 `+  splitNodeIntoSentences,` 一行，切句逻辑一字不动
- `grep -rn "正则与旧实现逐字符一致" src` → 无残留（exit 1）；splitHighlightSentences 全库无代码引用（仅 aiHighlight 文件头历史记载）
- 送审管线（useAIProofread/textBlockSplitter/aiProofreadService/aiResponseParser）git 零触碰；sentenceId 格式 type-line-seq 与 lineNumber 键语义不动；layout/exporter/parser/sanitize 零改动

### 门禁

npm test 17 文件 / 372 用例全绿（较开工 +1 文件 / +28 用例——网随工作增长）；npm run lint 零 error；npm run build exit 0。

### 顺带发现

无新增入池项（0048/0049 已由勘探入池；「附带语义」字体类域随句边界移动属条款2 预定行为面，已在差异说明留档，不另入池）。

### 交卷口径

- 任务概况单元2 状态按 ctl 咬合规则置「已派（实施完成待验收——验收结论回填后置「完成」）」，仿 task-0006 交卷口径（验收结论为技术负责人亲验区）。
- 复跑 ctl task check task-0007 全绿（见收尾核验）。
