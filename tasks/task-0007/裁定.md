# 裁定｜task-0007 切句对齐：高亮侧复用送审切句

- 裁定人：技术负责人（夜班，用户不可及——按交接决策规则代拍板，醒后可复核推翻）
- 日期：2026-09-12

## 裁定1｜复用形态采 A（新增 export），弃 C（抽共用层）

- 必裁信号：第二实例——`splitHighlightSentences` 即送审切句的第二实例（预裁方向＝消灭它）。
- **A（选定）**：`splitNodeIntoSentences` 新增 export（零逻辑改动），高亮侧 import 消费；`splitHighlightSentences` 删除或退化为转发。sentenceSplitter 行为一字不动（红线「对齐切句边界而非重写」的最忠实现）。
- C（否决）：抽共用层须重构切句代码本体——触碰红线（送审切句行为必须逐字节保持），且 export 单函数已达成单一真值，重构无增量收益。
- 微基准 2-4 倍慢（绝对量微秒级、渲染路径每节点一次）——不构成否决理由（工作量/性能均非否决理由，收益论证才是否决理由）。

## 裁定2｜条款2/3/4 的行为展开方向确认（用户红线预裁的忠实执行）

- 标题体 seq 连续（自 2 起）＋首句边界同源（？！；……入界）——消灭 id 碰撞与 k/k+1 错位。
- SIGNATURE/DATE/REMARK 整节点 seq=1（镜像 NON_SPLITTABLE_TYPES）。
- 多段标题统一查合并句 id。
- 三者均为「高亮查询键向送审 sentenceId 语义对齐」——0013 的本义（高亮与送审错位修复），sentenceId 格式本身不动（红线）。

**方案三问**：行为＝AI 高亮罩句与送审句子全对齐（差异清单 1~7 号边界一致化＋结构性错位四项消灭）——这是用户可感修复（高亮罩错句的场景纠正）；验证＝对照测试＋快照有意更新（previewStructure 2 条）＋三绿；回滚＝git revert 单提交，无可逆面。

## 附记

- 顺带发现 0048（首现即弃）/0049（标题合并去空白）已入池——送审侧现状缺陷，本任务高亮侧照单继承（双侧一致优先于单侧理想化），分诊留用户。
- 明确不做第 5 条的 0046 池备注改写归分诊——高亮碎片面向量消失后 0046 的表述需更新，不入本任务。
