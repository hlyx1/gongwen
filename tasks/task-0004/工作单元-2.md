# 工作单元-2｜task-0004 对齐族八项：开关翻转＋预览接线（实施）

## 底座＝角色提示词原文

> 由 `ctl dispatch` 自动拼接（prompts/03-工程师.md），以下为派工时的最终全文留档——重启与验收可回查当时派的到底是什么。

# 提示词 03｜工程师（技术负责人派出）

> 用法：技术负责人在契约定稿后派出，可多次派出（一项任务通常多个实施单元）。

你是**工程师**，按工作单元实施。你是在为**结构质量**工作，不是在堆功能——产出以结构质量＋行为保持衡量，不以"功能跑起来"衡量。

## 你要做的

1. **读工作单元六要素**：范围、边界、必读文件、交付物——清单外事项一律不做
2. **测试先行——无锁定，不动手**：要触碰的每一块行为，动手前先有测试锁住它。重构类：对着现状写特征测试、看着它绿再动、全程保持绿；新功能类：**详细需求的条款与验收法就是考卷**——先写测试（红）再实现到绿。出题权在外部，你不给自己的实现出题
3. **小步提交，每步回路绿**——回路＝只跑本次触碰面直接相关的测试＋项目最快的静态检查；**全量（全测试＋构建门禁）收尾一次**，交卷必须全量绿。代码与档案同批提交——档案含受影响的 Agent 文档（AGENTS.md 等）与文档集群，改到与代码一致
4. **顺带发现**：范围外的问题、可疑缺陷，一行一条记入"顺带发现"——只上报不顺手修，并 `ctl backlog add "…" --source 工程师 --status 待议` 逐条入池（池是唯一入口，01 按状态分诊）。**特别盯逻辑同构**：你正在写的东西若与库中某处逻辑形状相同（同一类资源、同一个动作、同一条链路——哪怕没有一行代码相同），这就是上报信号：交技术负责人开裁定（就地实现 vs 抽共用层），不擅自改道、不就地扩界
5. **收尾**：跑工作单元指定的验收命令；对照交付物清单逐件自查在盘；`ctl task check <task>` 全绿再交卷——你也动了档案（工作单元尾部、Agent 文档），别把结构红灯交给技术负责人；向技术负责人汇报一行＋证据路径

## 纪律

- 发现实际范围超出工作单元所述 → 停下上报升级，不自行扩权
- **测试网随工作增长**：单元结束时，被覆盖的行为面只能比开工时更大
- 确实无法自动验证的面（如纯视觉），显式声明并给出人工核验步骤，不得静默跳过


## 附录·六要素

1. **必读文件**：
   - tasks/task-0004/详细需求.md（需求条款＝考卷,验收法即判卷标准）
   - tasks/task-0004/勘探.md（关键文件地图——省探查时间）
   - tasks/task-0004/裁定.md（选定方案与被否理由——照裁定实施,不擅自改道）
   - 代码库文件（技术负责人补入——取自勘探文件地图与裁定落点）:
2. **一句话范围**：按详细需求条款1/2/4/5/6/7/8/9/10 实施七项对齐（**条款3/0002 已裁定剔除不执行**）——0001＋0022（翻开关＋预览接线：ROLE_INLINE_CLASS 共用层、新类 .a4-body-punctuation、首句查询域聚合、仅标点 span、跨 run 句序号偏移）、0004（纯开关翻转）、0003（红线 1.875px/4px）、0023（案 B：决策层字体来源改 config＋预览去 TNR）、0025/0026（cssVars 接线）。各项遵循条款9 流程：先快照锁定现状→变更→更新受影响快照→留「变更前后差异说明」。
3. **边界（不做）**：详细需求「明确不做」9 项＋裁定裁定1（0002 冻结）；红线＝切句正则与 sentenceId 格式一字不动、AST 不动、导出 13 条快照零变更、禁止散落 if、`:scope > p` 段落级 DOM 不变。
4. **交付物**（逐件列出——验收时对照在盘逐件核）：①七项代码/快照变更（落点以勘探.md §三 文件地图为准）；②npm test 全量绿＋npm run build 绿输出记录；③七项各自「变更前后差异说明」（写入实施记录，供用户醒后抽查）；④顺笔修 A4PageNumber.tsx:10 注释编号失实（0040）；⑤工作单元尾部实施记录与提交哈希。
5. **验收依据**：回路绿逐步验（触碰面相关测试），全量（全测试＋构建门禁）收尾一次绿；网随工作增长（触碰的行为面有测试）；留痕（工作单元在档、同批提交）
6. **身份提醒**：你是为结构质量工作的，不是为堆功能工作的；产出以结构质量＋行为保持衡量。**夜班特别约束：用户在睡觉、不可提问——拿不准照裁定与详细需求自决，超边界发现只入池不搭车修。本任务是「有意行为变更」任务但导出侧零字节变化是机械红线（13 条导出快照）；小步提交（每项或每两三项一笔，代码+快照+档案同批）；分页度量容器与页面共享渲染器——run 拆分成 span 后须跑全量预览快照确认无段落高度漂移。**

## 验收结论（技术负责人回填）

通过（经复核单元3）：复核.md 总体判断通过——五裁定照裁实施、六红线亲证成立（切句正则逐字符一致/导出快照零字节/AST与sanitize零触碰/无散落if/段级DOM不变）、测试网真实增长（344用例含0001拆分形态/0022字节级比对/0023接线用例）；复核发现轻缺陷（正文族裸句span缺key）已由验收人整改（Fragment key 包补，344全绿快照零变化）

---

## 实施记录（工程师回填·2026-09-12 夜班）

### 提交清单

| 哈希 | 内容 |
|---|---|
| f3e06bd | 档案：派工在档（task-0004/ 全目录＋待办池立项标记） |
| d47e486 | 对齐(0001/0004)：开关翻转＋ROLE_INLINE_CLASS 共用层＋.a4-body-punctuation＋首句聚合（快照 12 条＋buildLayout preview 1 条同批） |
| 3796731 | 对齐(0022)：案二最小 DOM 接线＋句序号节点内连续（aiHighlight 抽切句函数，正则一字未动） |
| 0ed911f | 对齐(0003/0023)：红线值自 constants 派生＋CSS 同步；页码字体案 B 两侧同源 config（导出 13 条快照零变更自证） |
| ccb4574 | 接线(0025/0026)：cssVars 注入＋var 消费（新增 2 用例，Preview 3 条快照） |
| 506eed8 | 档案(0040/AGENTS)：注释勘误＋AGENTS.md 同步＋顺带发现入池（待办-0041） |

### 七项「变更前后差异说明」（条款9：供用户醒后抽查）

1. **0001 三级标题序号句点字体**（d47e486）：预览「1.」的半角句点从跟随三级标题字体（整句单 span）→ 正文字体独立 run。DOM：`<span class="a4-h3-inline">1.加强组织领导。</span>` → `<span class="a4-h3-inline">1</span><span class="a4-body-punctuation">.</span><span class="a4-h3-inline">加强组织领导。</span>`。开关 `heading3DotFont.preview: follow-heading3 → body-font`。**默认配置下不可感**（h3 默认字体＝正文 仿宋_GB2312；仅用户改 h3 或正文字体为不同值时可感）。首句 seq=1 高亮查询域＝首句全文本域（聚合至「。」边界的 runs，AI 快照高亮位置不变自证）。
2. **0004 全角句点拆分**（d47e486）：「1．标题」的「．」从两侧均不拆（跟随标题字体）→ 两侧均与半角同法拆分（`heading3FullwidthDot: 双侧 no-split → split`，runs.ts split 分支既有实现零新增代码）。**默认配置下不可感**（同 0001）。新增 buildLayout 用例锁定 `[序号, ．(bodyPunct), 内容]`。
3. **0003 版头红线粗细间距**（0ed911f，裁定5 案 A）：预览红线 `border-bottom: 2px → 1.875px`（15/8pt，-6%，肉眼几乎不可辨）、`margin-top: 8px → 4px`（80twips，**-50%——红线到上方发文字号/签发人行距离减半，版头启用时明显可感**）。开关值自 constants 派生（`HEADER_SEPARATOR_SIZE_EIGHTH_PT/8`、`HEADER_SEPARATOR_BEFORE_TWIPS/20`）。人工核验：启用版头的样例文档，预览与导出 docx 红线并排目测粗细与上方间距一致。
4. **0022 时间冒号分段**（3796731，裁定3 案二）：预览正文族「9:00」的半角冒号从整段宿主渲染 → 独立 `.a4-body-punctuation` span；**无时间冒号的段落 DOM 逐字节零变化**（快照 diff 仅含冒号处标点 span，新旧快照对无冒号段落抽取比对一致）。预览视觉：标点 span 字体栈与段落字体栈同值 → **默认配置下预览视觉零差异**（本项价值＝机制/DOM 接线对齐导出侧既有行为；导出侧零改动）。句序号节点内跨 run 连续＝「全文切句＋句区间映射」实现（切句正则自 aiHighlight 原样抽出为 splitHighlightSentences，正则与 sentenceId 格式一字未动；renderSentenceHighlight 由段渲染器收编，行为等价由 AI 快照字节保持自证）。
5. **0023 页码字体**（0ed911f，裁定2 案 B）：预览页码「— 1 —」字体栈 `'Times New Roman', var(--page-number-font,'宋体') → var(--page-number-font,'宋体')`——数字与一字线从 TNR 字形变宋体字形，**默认即可感**（衬线数字→宋体风格）。导出侧字体来源 `PAGE_NUMBER_FONT 常量 → config.specialOptions.pageNumberFont`（默认 '宋体' 不变 → **导出 13 条快照零字节变更**）；顺带修复「页码字体设置导出侧静默失效」——改设置后两侧同源生效（新用例锁定）。人工核验：设置面板改页码字体「黑体」→ 预览页码随变、重新导出 docx 页码同为黑体。
6. **0025 三级标题字号**（ccb4574）：预览 h3 字号 `var(--body-size,16px) → var(--h3-size, var(--body-size,16px))`＋cssVars 注入 `--h3-size`。**默认零视觉差**（h3 默认 16＝正文 16）；用户改三级标题字号后预览从「不变」变为随设置变化（与导出同步）。人工核验：设置改三级标题字号 15 → 预览随变；恢复默认 16 与基线无差异。
7. **0026 主送机关字体/字号**（ccb4574）：`.a4-addressee` 字体/字号从固定跟随正文 → `var(--addressee-font, var(--body-font,…))`/`var(--addressee-size, var(--body-size,16px))`＋cssVars 注入。**默认零视觉差**（addressee 默认值＝正文值）；仅接中文字体槽（英数字体槽全族未接线＝待办-0039）。「标题字体」区 hint「同时作用于预览与导出」由失实变真（0025/0026 落地）。人工核验：设置改主送机关字体黑体/字号 15 → 预览主送行随变。

**条款3（0002）按裁定1 冻结未执行**：开关默认值维持现状（preview 4.2% / docx 397twips），定标流程已注记于待办池 0002 行。

### 快照与测试变更统计

- previewStructure：**13/14 条变**（10 条 A4Page 全节点样例＋3 条 Preview；空文档 A4Page 1 条不变）——变化内容＝0001 h3 三 span 拆分＋0022 冒号标点 span＋0025/0026 cssVars 注入（Preview 3 条含空文档）。与勘探 §四预期一致。
- buildLayout：preview 全节点快照 1 条变（h3/冒号 run 拆分）；docx 快照与版头开 docx 快照不变。
- **导出 docxBuilder 13 条快照零字节变更**（`git diff f3e06bd -- src/exporter/__tests__/__snapshots__/` 为空）——机械红线达成。
- 测试网增长：331→前值 340→**344 用例**（新增：0004 全角 buildLayout 用例、0023 config 接线用例、0025/0026 cssVars 注入断言 2 条；deviations/styleFactory 既有断言按新默认值更新）。
- 快照更新与代码同批提交（B/C/D/E 各 commit 均含代码＋测试＋快照），无单独快照提交。

### 红线自查

- 切句正则一字未动（`splitHighlightSentences` 内 `/[^。！？；…]*[。！？；…]|[^。！？；…]+/g` 与迁移前逐字符一致）；sentenceId 格式（nodeType-lineNumber-localSeq）一字未动。
- AST／parser／sanitize 零触碰；`:scope > p` 段落级 DOM 不变（run 拆分仅限段落内 inline 层，快照证明 `<p>` 类名与层级不变）。
- 禁止散落 if 达成：七项全部经 deviations.ts 开关默认值（0001/0004/0022 翻转、0003/0023 值/形态更新）或决策层统一出口（0023 config 接线、0025/0026 cssVars 注入），渲染器内无按配置分支的硬编码偏差。
- 0002 冻结不执行（裁定1）；超边界发现入池待办-0041（标题时间冒号标点 span 预览未接线——条款5 范围外残留差异，只上报未搭车）。

### 收尾门禁（条款10）

- `npm test`：**344/344 全绿**
- `npm run build`：**✓ built in 2.18s（466 modules transformed），零 error**
- `npm run lint`：**零输出（零 error）**
