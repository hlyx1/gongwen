# 复核｜task-0004 单元2：七项预览/导出对齐实施

- 复核员：05-复核员（技术负责人派出，工作单元-3）
- 日期：2026-09-12
- 复核对象：提交 d47e486（0001/0004）/ 3796731（0022）/ 0ed911f（0003/0023）/ ccb4574（0025/0026），基线 f3e06bd；附带核查 506eed8（0040 注释勘误＋AGENTS.md 同步）
- 方法：亲读逐笔 diff、快照字节级脚本比对、亲跑验收命令；未引用工程师转述

## 一、机械面

- `ctl task check task-0004`：**11 OK 全绿**（PASS）

## 二、红线真伪（逐项亲证）

| 红线 | 结论 | 证据 |
|---|---|---|
| 切句正则一字不动 | 通过 | `git diff f3e06bd..HEAD -- src/components/Preview/aiHighlight.tsx`：正则 `/[^。！？；…]*[。！？；…]\|[^。！？；…]+/g` 与 trim 口径均为 diff 上下文行（未变）；`splitHighlightSentences` 为原 `renderSentenceHighlight` 切句部分原样抽出 |
| sentenceId 格式一字不动 | 通过 | 拼接收编至 renderContentFlow.tsx 既有函数 `sentenceId(block, seq)`（`sourceType + '-' + sourceLineNumber + '-' + seq`），与旧 `sourceType + '-' + sourceLineNumber + '-' + sentenceSeqInNode` 逐字符一致；`utils/sentenceSplitter.ts` 零 diff |
| 导出 13 条快照零字节 | 通过 | `git diff f3e06bd..HEAD -- src/exporter/__tests__/__snapshots__/` 为空（0023 案 B 默认 config 下字体来源值＝旧常量值，快照绿自证） |
| AST/sanitize/parser 零触碰 | 通过 | `git diff f3e06bd..HEAD -- src/parser/ src/types/ast.ts src/utils/sanitize.ts` 为空；`utils/`、`hooks/usePagination.ts` 整目录零 diff |
| 无散落 if | 通过 | renderContentFlow.tsx 无 `if (config.` 类配置分支；七项全经 deviations.ts 开关默认值（0001/0004/0022 翻转、0003/0023 值/形态更新）或决策层统一出口（0023 config 来源、0025/0026 cssVars 注入） |
| `:scope > p` 段级 DOM 不变 | 通过 | usePagination.ts:165 选择器原样；快照归一化比对（仅剥 `.a4-body-punctuation` span）后 30 行全等＝`<p>` 类名层级零变化；run 拆分仅限段落内 inline 层 |

## 三、五裁定符合性

1. **裁定1（0002 冻结）**：`pageNumberVertical` 默认值未动（preview 4.2% / docx 397twips）——通过。
2. **裁定2（0023 案 B）**：`buildPageNumberLayout` 来源改 `config.specialOptions.pageNumberFont`；预览 `.a4-footer` 栈去 TNR 改 `var(--page-number-font, '宋体'), serif`；开关 primary 语义改 config 来源；案 A 未借尸还魂——通过。
3. **裁定3（0022 案二最小 DOM）**：案一（全 run span）未出现；相邻同宿主 run 合并纯文本、仅 bodyPunct 独立 span；跨 run 句序号经「全文切句＋区间映射」实现（切句在段拼接全文上一次完成，sentenceId 用全文句序——裁定允许的调用约定扩展）；案二实现代价如实兑现——通过。
4. **裁定4（ROLE_INLINE_CLASS 共用层）**：泛化为 content/attachment 双槽全角色表；**attachment 槽值与旧 ATTACHMENT_INLINE_CLASS 逐字一致**（`a4-attachment-text`/`a4-attachment-punctuation`），`renderAttachmentContent` 消费泛化表——附件路径 DOM 字节保持；0001 用新增语义化类 `.a4-body-punctuation`（未复用 a4-paragraph-inline）——通过。
5. **裁定5（0003 案 A）**：CSS 单源直接改值 `border-bottom: 1.875px; margin-top: 4px`；开关值自 constants 派生（`HEADER_SEPARATOR_SIZE_EIGHTH_PT=15 → /8=1.875`、`HEADER_SEPARATOR_BEFORE_TWIPS=80 → /20=4`，constants.ts 本任务零触碰）；案 B（组件内联 style）未出现——通过。

## 四、条款验收法覆盖（条款1/2/4/5/6/7/8/9/10；条款3 裁定1 冻结）

- **条款1（0001）**：`HEADING_3：preview 决策拆分序号句点（0001 对齐后）` 断言 `['1','.','加强组织领导。']`＋role/bodyPunct 字体断言（真锁定，非空断言）；首句查询域聚合＝`indexOf('。')` 与决策层 `splitHeadingSentenceRuns`/`splitHeading3Runs` 同口径（「首『。』恒落 run 边界」声明成立——决策层先按「。」拆首句再序号拆分）；快照 13/14 条变＋空文档 A4Page 1 条不变（亲证：14 条快照 13 变、行 21 空文档条目未变）——**覆盖**。
- **条款2（0004）**：新增 `HEADING_3 全角句点（0004 对齐后）：docx/preview 均拆分 [序号, ．(bodyPunct), 内容]` 双渲染器断言＋styleFactory 同步更新；导出快照零变更——**覆盖**。
- **条款4（0003）**：断言更新为 1.875/4；0ed911f 中 A4Page.css 变更仅红线规则＋页脚字体栈（0023）两处，红线规则仅此一处——**覆盖**。
- **条款5（0022）**：双 split 断言；快照 diff 字节级验证＝**12 行差异全部为标点 span 插入**（0022 前标点 span 16 个 `.`，0022 后新增 32 个 `:`，纯文本剥离标签比对 0 差异）；AI 高亮 2 条快照高亮 span 数与各 span 文本逐一相同（sentenceId 语义与高亮位置不变）——**覆盖**。
- **条款6（0023）**：默认 config 断言原样通过＋导出快照零变更＋新用例 `页码字体接 config（0023 案 B）：改 pageNumberFont 后四槽随配置、两渲染器同源`——**覆盖**（人工目测步骤已在实施记录声明，留用户醒后核验）。
- **条款7/8（0025/0026）**：新增 2 条 cssVars 注入断言（`--h3-size:15px`/`--addressee-font:黑体`）＋Preview 3 条快照；`.a4-h3`/`.a4-addressee` 改 var 消费、缺省回退正文值——**覆盖**（人工步骤同上声明）。
- **条款9（流程）**：四笔实施提交均含代码＋测试＋快照（无单独快照提交）；七项差异说明＋提交哈希清单在档（783d642）——**实质覆盖**；形式偏差见问题清单第 2 条。
- **条款10（收尾门禁）**：亲跑 `npm test`＝**344/344 全绿**（16 文件）；`npm run build`＝**✓ built in 2.10s 零 error**；`npm run lint`＝**零输出**——**覆盖**。

## 五、测试网真伪

- 331 → **344**（净增 13：0004 全角、0023 config、0025/0026 cssVars 2 条为新增用例；0001/0003/0022/0023 等既有断言按新默认值改写并新增「开关覆盖生效（旧现状可回滚）」反向用例）。网随工作增长成立。
- 抽查四点全真：①0001 拆分形态断言含 role/字体四槽（非形状匹配）；②0022 无冒号段落零变化经字节级脚本比对证实；③0023 config 接线用例双渲染器同源断言；④快照与代码同批（逐笔 stat 核对）。
- 无 skip、无空断言、无只测快乐路径（含全角/覆写回滚/无句子退化等边角）。

## 六、七项差异说明与实际 diff 一致性

逐项核对工作单元-2 实施记录第 1~7 条与对应 diff：**全部一致**（0001 三 span 形态、0004 双侧 split、0003 值 1.875/4、0022 仅标点 span＋无冒号零变化、0023 栈/来源两侧同源、0025/0026 cssVars 注入与 var 消费、0002 冻结注记）。`.a4-body-punctuation` 字体栈与 `.a4-paragraph` 逐字相同（「默认零视觉差」声明成立）。

## 七、顺带核查

- 待办-0041（标题时间冒号标点 span 预览未接线——条款5 范围外残留差异）已入池且描述准确：预览标题走 `renderTitleHighlight` 整段渲染、导出侧决策层 colonSplitOn 分支含标题，标题含 9:00 时确有残留差异——工程师如实上报未搭车，处置正确。
- 0040 注释勘误（A4PageNumber.tsx 0022→0002）无行为变化。

## 八、发现的问题（按严重度）

1. **【轻缺陷·建议补修】React key 警告新引入**：`src/components/Preview/renderContentFlow.tsx` `renderSegmentsWithHighlight` 有 AI 结果分支中 `elements.push(renderSentenceSpan(piece, result, ai))`（hostWrapperClass=null 的正文族路径）push 的句子 span 无 key；旧实现 `sentences.forEach` 的 span 带 `key={idx}`。仅 AI 结果非空时触发（previewStructure 2 条测试 stderr 多条警告），**DOM 输出/快照/行为零影响**（renderToStaticMarkup 不依赖 key）。基线 f3e06bd 同测试 key 警告计数 0（亲测），证实新引入。建议：补一层带 key 的包装或为 renderSentenceSpan 增加 key 传参——可由技术负责人并入下一小笔（如与 0041 处置同批）。
2. **【流程注记·非缺陷】条款9 字面口径偏差**：验收法写「每项变更提交同时含代码+快照+档案」，四笔实施提交未各自携带任务档案（差异说明七项在 783d642 尾批聚合提交）。实质（禁单独快照提交、差异说明在档可抽查）达成；由技术负责人裁量是否要求后续任务逐笔携带。
3. **【注记】人工目测步骤待用户醒后执行**：条款6/7/8 的设置面板改值目测（页码黑体、h3 字号 15、主送黑体）无法在夜班复核中执行，实施记录已按工程师纪律显式声明步骤。

## 九、总体判断

**通过**（附 1 项轻缺陷建议补修＋2 项流程注记）。五裁定全部照裁实施、被否方案无借尸还魂；六条红线全部亲证成立；测试网真实增长且真锁新行为；导出侧零字节红线达成；七项差异说明与 diff 一致。

## 十、文档更新清单（复核员收尾，同批提交）

- `AGENTS.md` 目录树注释（原 88 行）：`deviations.ts # 已知预览/导出偏差显式开关（默认＝现状）` → `（默认＝task-0004 对齐值，0002 除外）`——该行未随 506eed8 同步，为残留失真。
- `AGENTS.md` layout 模块详解（原 203 行）：`偏差开关：deviations.ts 集中登记已知预览/导出偏差（默认值＝现状）` → `（默认值＝task-0004 对齐值，0002 挂起维持现状）`——同上。
- 其余 AGENTS.md 失真处（数据流第 45 行 bullet、renderContentFlow 描述、三级标题句号条款、测试清单 344）已由 506eed8 同步到位，复核确认数字吻合（分项加总＝344，previewStructure 16 tests 亲跑相符）。
