# 详细需求｜task-0004 对齐族八项：开关翻转＋预览接线

> 定稿即冻结：后续改动走新裁定（裁定.md），不回改本件。
> 需求条款用列表行书写，每行必带【验收：…】——验收法写 vitest 用例名或人工步骤。
> 完整性由 `ctl check` 机械把关：离开澄清中前，条款行缺验收法、待澄清占位行残留、「明确不做」缺节，均为红。
> 勘探依据：`tasks/task-0004/勘探.md`（现状值、文件行号、快照清单、挂起待裁双案均以其为准）。

## 需求条款

- 条款1（0001·三级标题序号句点字体·开关翻转＋预览接线）：`src/layout/deviations.ts` `DEVIATION_SWITCHES_DEFAULT.heading3DotFont.preview` 由 `'follow-heading3'` 翻转为 `'body-font'`（docx 分支不动）；`renderContentFlow.tsx` `renderHeadingRuns` 改为按 `run.role` 映射 inline 类名——将既有 `ATTACHMENT_INLINE_CLASS`（:55-58）泛化为全角色共用表（附件/标题/正文三处消费同一表，附件既有类名值不变；若技术负责人裁「就地实现」则允许三处映射并存，验收法不变）——`heading3` 角色→`a4-h3-inline`、`bodyPunct` 角色→正文字体 inline 类（新增 `.a4-body-punctuation { font-family: var(--body-font,…) }` 或复用 `a4-paragraph-inline`，实施取其一并在快照 diff 中可辨）；首句 seq=1 的 AI 高亮查询域保持「首句全文本域」（聚合至「。」边界的 runs，不得退化为仅首 run/序号）——切句正则与 sentenceId 格式一字不动（红线）。【验收：vitest「0001 heading3DotFont：预览 follow-heading3 / 导出 body-font」断言更新为双 body-font；vitest「HEADING_3：preview 决策不拆分序号句点（0001 预览现状）」断言反转为拆分形态（[序号(h3), '.'(bodyPunct), 内容(h3)]）；previewStructure 14 条快照按预期差异更新后全绿（预期 13 条变、空文档 A4Page 1 条不变）；npm test 全量绿】
- 条款2（0004·全角句点拆分·纯开关翻转）：`DEVIATION_SWITCHES_DEFAULT.heading3FullwidthDot` 双侧由 `'no-split'` 翻转为 `'split'`（runs.ts:106 split 分支正则 `/^(\d+)([.．])(.*)$/` 已实现，决策层零新增代码；preview 分支生效依赖条款1 的接线）。新增全角句点三级标题（如「1．加强组织领导。」）的决策层用例。【验收：vitest「0004 heading3FullwidthDot：两侧均 no-split」断言更新为双 split；新增用例「HEADING_3 全角句点（0004 修复后）：docx/preview 均拆分 [序号, ．(bodyPunct), 内容]」绿；导出 docxBuilder 13 条快照 git 零变更（现有样例无全角句点，半角行为零改动）】
- 条款3（0002·页码纵向位置·实测定标后翻转，默认案 A）：先人工定标——默认配置导出 docx，在 Word/WPS 打开，量测页码数字底边距页底的实际 mm（导出机制＝docx 库默认 footer margin 708twips＋段前距 397twips，换算候选区间 4.2%~8.2% 依赖渲染器锚定语义，禁止跳过实测按字面 7mm 生搬）；按 72dpi（1mm≈2.835px、页高 297mm≈842px）把定标值换算为 `bottomPercent` 写回 `pageNumberVertical.preview`，`A4Page.css` `.a4-footer` 的 `bottom` 同步同值。若技术负责人裁案 B（A4PageNumber 消费 `layout.pageNumber.vertical` 内联 style）则 CSS 侧改为组件侧接线，验收法互换。【验收：人工步骤——定标记录（实测 mm→换算式→bottom% 值）写入本任务实施记录；定标后预览页码与导出 docx 并排目测纵向位置一致（±1 行高内）；vitest「0002 pageNumberVertical：预览 css-bottom-percent 4.2 / 导出 footer-spacing-before 397」断言按定标值更新后绿】
- 条款4（0003·版头红线粗细间距·值对齐，默认案 A）：`redSeparator.preview` 更新为 `{ thicknessPx: 1.875, marginTopPx: 4 }`（导出侧 15/8pt=1.875pt、80twips=4pt；预览 72dpi 下 1pt=1px）；`A4Page.css` `.a4-header-separator` 改 `border-bottom: 1.875px solid #e00000; margin-top: 4px`。案 B 之争同条款3。【验收：人工步骤——启用版头的样例文档，预览红线与导出 docx 红线并排目测粗细与上方间距一致；vitest「0003 redSeparator：预览 css 2px/8px / 导出 paragraph-border 15/80」断言更新为 1.875/4 后绿；A4Page.css 变更仅此一处红线规则（diff 核对）】
- 条款5（0022·时间冒号分段·开关翻转＋预览接线，默认案二最小 DOM）：`timeColonSplit.preview` 由 `'no-split'` 翻转为 `'split'`；`renderContentFlow.tsx` 正文族（主送/正文/署名/日期/备注——即 renderParagraphBlock 现走 `blockText()` 整段分支的 sourceType）引入 run 级渲染，默认采用最小 DOM 案：相邻同 `run.role` 的 runs 合并后按纯文本渲染、仅 `bodyPunct` run 包正文字体 inline span（与条款1 共用 ROLE_INLINE_CLASS 表）——**无时间冒号的文档 DOM 结构零变化**；若技术负责人裁案一（全 run span）则整段每 run 包 span，验收法互换。两案共同硬约束：sentenceId 冻结——切句输入文本、切句正则（aiHighlight.tsx:41）、id 拼接格式零改动；跨 run 的句序号必须保持节点内连续（实施可为句渲染提供 run 前累计句数偏移的调用约定扩展，但不得改切句逻辑）。【验收：vitest「0022 timeColonSplit：预览 no-split / 导出 split（单元5 接线补设，裁定 §八）」断言更新为双 split；previewStructure 快照更新后全绿且 diff 仅含时间冒号处新增标点 span（案二口径）——「会议时间为9:00至11:30」段落外其余段落结构零变化；AI 高亮 2 条快照（含 PARAGRAPH-5-1 命中样例）中高亮 span 位置与 sentenceId 语义不变；npm test 全量绿】
- 条款6（0023·页码字体·默认案 B）：导出侧接线用户配置——`layout/index.ts` `buildPageNumberLayout` 页码字体来源由常量 `PAGE_NUMBER_FONT` 改为 `config.specialOptions.pageNumberFont`（默认 '宋体'，默认产物四槽仍全宋体）；`A4Page.css` `.a4-footer` 字体栈由 `'Times New Roman', var(--page-number-font, '宋体'), serif` 改为 `var(--page-number-font, '宋体'), serif`（去 TNR 优先）；`deviations.ts` `pageNumberFont.preview` 形态登记同步（`{ mechanism: 'css-stack', primary: … }` 的 primary 语义改为 config 来源，文件头注释同步）。若技术负责人裁案 A（严格恒宋体）：导出侧不接 config、预览栈改 `'宋体', serif` 硬值、SettingsModal 页码字体控件一并移除——三处改动互换，验收法第 2/3 条互换。【验收：vitest「页码开：四号宋体、空一字 280、奇右偶左、— X — 格式」默认 config 下断言原样通过（font 仍全宋体）；导出 docxBuilder 13 条快照 git 零变更；人工步骤——设置面板改页码字体为「黑体」后，预览页码字形随变且重新导出 docx 页码字体同为黑体（两侧同源生效）；vitest「0023 pageNumberFont：预览 CSS 栈（TNR 优先）/ 导出四槽宋体」断言更新后绿】
- 条款7（0025·三级标题字号接线）：`Preview.tsx` cssVars 增 `'--h3-size': ${config.headings.h3.fontSize}px`（:69-74 区既有 --h3-font 之后）；`A4Page.css` `.a4-h3` 的 `font-size` 由 `var(--body-size, 16px)` 改为 `var(--h3-size, var(--body-size, 16px))`；行距不动（导出/预览两侧 h3 行距均＝正文行距 exact，现状一致）。【验收：人工步骤——设置面板改三级标题字号（如 15px），预览三级标题字号随变（改动前不动）；恢复默认 16 后预览与基线截图无差异；previewStructure 快照（Preview 3 条，preview-scroll style 新增 --h3-size）更新后全绿】
- 条款8（0026·主送机关字体/字号接线）：`Preview.tsx` cssVars 增 `'--addressee-font': config.headings.addressee.fontFamily` 与 `'--addressee-size': ${config.headings.addressee.fontSize}px`；`A4Page.css` `.a4-addressee` 的 font-family/font-size 改为 `var(--addressee-font, var(--body-font,…))`/`var(--addressee-size, var(--body-size, 16px))`。仅接中文字体槽与字号——英数字体（asciiFontFamily）槽全族未接线为待办-0039，本条款不扩。【验收：人工步骤——设置面板改主送机关中文字体（如黑体）与字号，预览主送行随变（改动前恒跟随正文）；恢复默认后预览与基线无差异；previewStructure 快照（Preview 3 条叠加 --addressee-*）更新后全绿】
- 条款9（快照与差异说明口径·流程红线）：每项变更遵循任务概况用户原话流程——先快照锁定现状→变更→更新受影响快照→在任务档案留「变更前后差异说明」供用户醒后抽查；受影响快照清单以勘探.md §四为机械口径（previewStructure 14 条中 13 条、buildLayout preview 快照 1 条、deviations 六开关断言、导出 13 条零变更）；快照更新与代码同批提交，禁止单独快照提交。【验收：git log 抽查每项变更提交同时含代码+快照+档案；任务档案（实施记录或任务概况）含八项各自的「变更前后差异说明」段；npm test 全量绿时 `git status` 无快照残留未提交】
- 条款10（收尾门禁）：八项全部完成后全量回归一次——`npm test`（全测试）与 `npm run build`（构建门禁零 error）双绿，代码与档案同批提交。【验收：两条命令输出全绿；提交含 tasks/task-0004/ 档案与全部代码/快照变更】

## 明确不做

- 不动 AST 结构、sentenceId 生成与切句边界：sentenceSplitter 切句逻辑、id 拼接格式、aiHighlight.tsx:41 切句正则一字不改（红线）；条款1/5 的高亮接线只改查询域聚合与调用约定，不碰切句。
- 不做待办-0013（A4Page 内联切句正则统一）、0016（〇 计宽）、0017（连续句点）、0012（DetectionPanel 硬编码颜色）、0018（twip 截断）——另立或待议，本任务不搭车。
- 不接线 h1~h3/addressee 的英数字体（ascii）槽（待办-0039 已入池另议）；条款8 仅接中文字体槽与字号。
- 不动 sanitize（全角「．」存活到解析器是条款2 的前提）；不动 parser/matchers。
- 不动 usePagination 分页测量机制与表格按段落测量现状（待办-0005 冻结）；run 拆分仅限段落内 inline 层，不得改变段落级 DOM（`:scope > p` 选择器依赖）。
- 导出产物字节级不变：半角句点/时间冒号/页码字体的导出侧行为零改动（0023 案 B 仅改字体来源，默认 config 下产物字节不变）——13 条导出快照是机械红线。
- 禁止散落 if：八项对齐全部经 deviations.ts 开关默认值或决策层统一出口，不得在渲染器内按配置分支硬编码偏差。
- 不动 SettingsModal UI 结构（条款6 案 B 下页码字体控件保留并恢复两侧生效；「标题字体」区 hint 在条款7/8 落地后自然由失实变真，不单独改文案）。
- 不做 eslint 专项治理（待办-0024 已立项 task-0001）。

## 待澄清

- 无——夜班不可提问，三项分叉已按默认方案收敛进条款（0002 定标值走条款3 实测流程、0023 默认案 B、0022 默认案二），并在勘探.md 标注「挂起待裁」双选项供技术负责人裁定；各默认案与备选案的验收法互换关系已写入条款，裁定不阻塞实施启动（仅条款3 需先定标后翻值）。
