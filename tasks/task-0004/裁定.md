# 裁定｜task-0004 对齐族八项：开关翻转＋预览接线

- 裁定人：技术负责人（夜班，用户不可及——按交接决策规则代拍板，醒后可复核推翻）
- 日期：2026-09-12

## 裁定1｜0002（页码纵向位置）本轮剔除挂起，回池待实测定标

**证据**：导出「7mm」是页脚段前距（footer-spacing-before 397twips），叠加 docx 库默认 footer margin 708twips 后，OOXML 锚定语义三种解读换算结果在 4.2%~8.2% 摆动——字面「4.2% vs 7mm」直觉差被夸大；其中最可信解读≈4.21%，与预览现状 4.2% 几乎重合，**现状可能已对齐**。定标必须导出 docx 在 Word/WPS 实测页码位置——夜班无 GUI 实测条件（GUI 测试环境限制在案）。

**处置**：条款3 冻结不执行；`待办-0002` 回池待议，注记定标流程（用户醒后：默认配置导出一份带页码 docx→Word/WPS 打开量页码数字底边距页底 mm→按 72dpi 换算 bottomPercent→若与 4.2% 差异可感再晋升实施；若≈4.2% 则直接销账「现状即对齐」）。**不盲改**：在两套机制语义未定标前翻转值，引入的是新偏差而非修复。

**方案三问（针对本裁定不动代码）**：行为＝零改动；验证＝不适用；回滚＝不适用。

## 裁定2｜0023 裁案 B——导出接线 config.specialOptions.pageNumberFont（设置项失联顺带修复）

- 案 A（恒宋体）否决理由：`config.specialOptions.pageNumberFont` 设置控件已存在且预览已消费，案 A 使其成死控件须砍用户功能——为对齐而砍功能本末倒置。
- 案 B：决策层页码字体来源改 config（默认 '宋体' 不变→导出 13 条快照零变化）；预览字体栈去 TNR 优先改 `var(--page-number-font, '宋体'), serif`。两侧同源跟随设置＝真对齐；顺带修复「页码字体设置导出侧静默失效」真实缺陷（勘探顺带发现④）。
- 方案三问：行为＝默认配置零变化（快照自证）；改页码字体后两侧同源生效（新行为，有测试/人工验收）；回滚＝git revert 单提交，无可逆面。

## 裁定3｜0022 裁案二（仅标点 span 最小 DOM）

- 案一（全 run span）否决理由：所有正文段落无条件多一层 span——previewStructure 快照大面积变化、无冒号文档 DOM 亦变，快照噪音与回归面不成比例。
- 案二：无时间冒号文档 DOM 零变化；快照 diff 仅含冒号处标点 span。实现代价（同宿主 run 合并＋跨 run 句序号偏移）由正确性买单。
- **跨 run 句序号连续性**红线重申：切句正则（aiHighlight.tsx:41）与 sentenceId 格式一字不动；允许为句渲染提供「run 前累计句数偏移」的调用约定扩展（纯传参，不改切句逻辑）。

## 裁定4｜run→inline 类名映射：抽象共用层（泛化 ATTACHMENT_INLINE_CLASS 为全角色表）

- 必裁信号：第二实例——附件 `ATTACHMENT_INLINE_CLASS`（renderContentFlow.tsx:55-58）为第一实例，0001 标题与 0022 正文为第二、三实例（同为「决策 run.role → inline CSS 类」）。
- **裁共用层**：泛化为全角色 `ROLE_INLINE_CLASS` 表，附件/标题/正文三处共用。附件既有类名值不变→附件路径字节保持（快照安全）。「为何现在抽象」：三实例同形状数据映射并存即是重复，新增角色（如 bodyPunct）一处登记三处生效。
- 0001 的 bodyPunct 类名**新增语义化类 `.a4-body-punctuation`**（不复用 a4-paragraph-inline——paragraph 类不应承载标点语义；快照 diff 可辨）。

## 裁定5｜0003 裁案 A（CSS 单源改值），不动组件

`.a4-header-separator` 直接改 `border-bottom: 1.875px; margin-top: 4px`（换算可靠：72dpi 下 1pt=1px）。案 B（组件消费决策参数内联 style）否决：twips 换算进组件＋含版头快照再变一批，与「CSS 单源」现状架构（A4PageNumber/A4HeaderSection 注释明言）相悖。开关值与 CSS 值两处人肉同步是既有架构代价，待办-0002 定标时如裁案 B 一并重估。

## 实施范围汇总（对照详细需求条款）

| 条款 | 处置 |
|---|---|
| 1（0001） | 照条款实施（ROLE_INLINE_CLASS 共用层＋新类 .a4-body-punctuation＋首句查询域聚合） |
| 2（0004） | 照条款实施（纯开关翻转） |
| 3（0002） | **冻结不执行**（裁定1 剔除挂起） |
| 4（0003） | 照条款实施（案 A 定死） |
| 5（0022） | 照条款实施（案二定死＋句序号偏移约定） |
| 6（0023） | 照条款实施（案 B 定死） |
| 7/8（0025/0026） | 照条款实施（纯预览接线） |
| 9/10（流程/收尾） | 照条款实施——差异说明按七项计（0002 剔除） |

## 附记

- 勘探顺带发现 0039（ascii 槽全族未接线）/0040（A4PageNumber 注释编号失实）已入池待分诊；0040 属一行注释勘误，实施单元可顺笔修（非冻结档案）。
- SettingsModal「标题字体」区 hint 在条款7/8 落地后由失实变真，不单独改文案。
