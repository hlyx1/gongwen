# 裁定｜task-0006 小对齐：〇计宽正则＋连续句点

- 裁定人：技术负责人（夜班，用户不可及——按交接决策规则代拍板，醒后可复核推翻）
- 日期：2026-09-12

## 裁定1｜0016 采「抽共享谓词」——CJK 宽度判定在 metrics.ts 内单源化

- 必裁信号：落点双选项（勘探上报）。metrics.ts:51（textWidthTwips）与 :64（textWidthEm）为同文件孪生正则，测试另有一份独立参考实现（metrics.test.ts:30/:37）。
- **裁共享谓词**：抽 `isCJKWidth`（或等义命名）单源谓词，两函数消费——同文件内孪生去重、判定口径单一真值；将来 0045（中文数字日期支持）落地时〇类字符扩展一处生效。
- **测试参考实现保持独立**（不 import 主谓词）：测试参考正则自行补 `\u3007`——参考实现与主实现共用同一谓词会使测试失去独立对照价值（同一 bug 两处同步错则测试恒绿）。修复后全库正则从三份收敛为「主一份＋测试独立参考一份」。
- 修复现网可见面为零（DATE_RE 只认阿拉伯日期——勘探勘正）——定性为单源谓词正确性修复＋0045 铺路，成本极小照做；署名偏移变化量（每〇 ±48.825twips/±0.155em）记入差异说明。
- 方案三问：行为＝现网零变化（〇 进不了链路）、谓词层正确性提升；验证＝metrics/styleFactory 基线 4 处同批更新后全绿；回滚＝git revert 单提交，无可逆面。

## 裁定2｜0017 挂起回池——产品分叉，夜班不猜

- 方向 A（逐个转全角「。。。」）：有下游反证（sentenceSplitter 会切成碎片句）。
- 方向 B（≥3 连转省略号「……」）：合国标形态，但阈值（≥3 还是 ≥2？）/映射（...→……还是 ……？）/count（两字符）三项微决策无据可依。
- GB/T 15834 只管成文标点用法、不管输入净化转换；库内无先例。**两个方向都说得通＝产品选择，按交接规则写清待问问题入池，待用户裁定**。条款3（0017）冻结不执行，池行回待议＋注记分叉问题（含 0046 联动提示：B 方向落地会使冻结切句正则的碎片句频率上升）。

## 附记

- 顺带发现 0045（DATE_RE 仅阿拉伯日期）/0046（省略号切句碎片）已入池，0045 是真正的「〇 可见」前置，留用户分诊。
- 实施范围＝条款1/2（0016 共享谓词＋基线同步）＋条款4（门禁收尾）；条款3（0017）冻结。
