# 详细需求｜task-0006 小对齐：〇计宽正则＋连续句点

> 定稿即冻结：后续改动走新裁定（裁定.md），不回改本件。
> 需求条款用列表行书写，每行必带【验收：…】——验收法写测试用例名或人工步骤。
> 完整性由 `ctl check` 机械把关：离开澄清中前，条款行缺验收法、待澄清占位行残留、「明确不做」缺节，均为红。

## 需求条款

- 条款1（0016：〇 按汉字全宽计）宽度计量两版 CJK 判定补 U+3007——`layout/metrics.ts` 的 `textWidthTwips`（:51）与 `textWidthEm`（:64）字符类同步补 `\u3007`，〇 按 1 个汉字宽计（1em / 1×charWidth），twip/em 两版行为一致；:46 「〇不在判定范围」注释同步改写。落点双选项（就地两处同步补 vs 抽共享谓词单源）行为完全等价，由技术负责人裁定（证据与影响面见勘探.md §五/§六）。【验收：layout/__tests__/metrics.test.ts 改写后用例「〇（U+3007）按汉字全宽计（两版一致）」断言 textWidthEm('〇')=1 且 textWidthTwips('〇',315)=315；署名用例「某某市人民政府办公室 × 二〇二六年九月十一日」无印=630/有印=1260；exporter/__tests__/styleFactory.test.ts 改写后用例「含〇日期（〇按全宽计）：630」——两文件全绿】
- 条款2（0016：测试基线同批同步）以下基线随代码同批更新，不允许只改代码不改基线（或反之）：metrics.test.ts:30 头注口径、:37 参考实现 referenceTextWidthEm 正则拷贝、:123-126 〇 现状断言、:141-142 二〇二六数值（581.175/1211.175→630/1260）；styleFactory.test.ts:578-583（581.175→630）。快照文件经 grep 核实零波及（全库快照不含〇）。【验收：npm test 全量绿（331 用例规模不减）；git diff 显示上述 4 处基线与 metrics.ts 代码同批提交】
- 条款3（0017：连续半角句点对齐——产品分叉，挂起不实施）现状「中文后仅首个句点转全角」（sanitize.ts:10 CJK_BEFORE_DOT＋:146-154 单趟替换，锁定于 sanitize.test.ts:37-41）的对齐方向存在两候选：A 连续句点逐个转全角（「等等...」→「等等。。。」）；B 三连以上转省略号（「等等...」→「等等……」，含阈值/映射/count 三项微决策）。国标仅规定成文标点用法（GB/T 15834 省略号形态……）未规定输入净化转换，库内无半角→省略号净化先例——无单解。按夜班约束（用户不可问）挂起，交技术负责人入池待用户裁定方向后回填具体行为条款转实施。【验收：人工——本任务全部实施提交中 src/utils/sanitize.ts 与 src/utils/__tests__/sanitize.test.ts 零变更（git diff 为空）；分叉两向证据链完整留档于勘探.md §二（含下游 sentenceSplitter/aiHighlight 影响分析与待办-0046 留证）】

## 明确不做

- deviations.ts 不新增开关：0016/0017 均非预览/导出偏差——两渲染器自 task-0001 起同吃 layout/metrics.ts 单源（错也一致）；sanitize 为渲染器无关的输入净化
- 0016 范围只补 U+3007：不扩其他 CJK 区段（扩展 B／兼容表意区等），不动 ASCII 0.69 系数、缩进公式与 0 下限钳制
- sanitize 其余行为锚点不动：36 用例中除连续句点用例（:37-41）外的 35 个行为不变（红线）
- 切句与 sentenceId 冻结不动：aiHighlight.tsx:35 切句正则、sentenceSplitter 边界、sentenceId 拼接格式
- 不做中文数字成文日期（二〇二六…）解析支持：已入池待办-0045 另议，本任务不因「触发面为零」反向扩 parser
- 不动 styleFactory.ts／docxBuilder.ts／renderContentFlow.tsx 等渲染器：0016 改动收敛在 layout/metrics.ts 与两测试基线文件

## 待澄清

- 无占位残留。0017 方向分叉不作为待澄清挂网（夜班用户不可问），已按夜班约束以条款3 挂起形式留档，交技术负责人入池待用户裁定；0016 无分叉（汉字全宽＝唯一单解）。
