# 工作单元-2｜task-0006 小对齐：〇计宽正则＋连续句点（实施）

## 底座＝角色提示词原文

> 由 `ctl dispatch` 自动拼接（prompts/03-工程师.md），以下为派工时的最终全文留档——重启与验收可回查当时派的到底是什么。

# 提示词 03｜工程师（技术负责人派出）

> 用法：技术负责人在契约定稿后派出，可多次派出（一项任务通常多个实施单元）。

你是**工程师**，按工作单元实施。你是在为**结构质量**工作，不是在堆功能——产出以结构质量＋行为保持衡量，不以"功能跑起来"衡量。

## 你要做的

1. **读工作单元六要素**：范围、边界、必读文件、交付物——清单外事项一律不做
2. **测试先行——无锁定，不动手**：要触碰的每一块行为，动手前先有测试锁住它。重构类：对着现状写特征测试、看着它绿再动、全程保持绿；新功能类：**详细需求的条款与验收法就是考卷**——先写测试（红）再实现到绿。出题权在外部，你不给自己的实现出题
3. **小步提交，每步回路绿**——回路＝只跑本次触碰面直接相关的测试＋项目最快的静态检查；**全量（全测试＋构建门禁）收尾一次**，交卷必须全量绿。代码与档案同批提交——档案含受影响的 Agent 文档（AGENTS.md 等）与文档集群，改到与代码一致
4. **顺带发现**：范围外的问题、可疑缺陷，一行一条记入"顺带发现"——只上报不顺手修，并 `ctl backlog add "…" --source 工程师 --status 待议` 逐条入池（池是唯一入口，01 按状态分诊）。**特别盯逻辑同构**：你正在写的东西若与库中某处逻辑形状相同（同一类资源、同一个动作、同一条链路——哪怕没有一行代码相同），这就是上报信号：交技术负责人开裁定（就地实现 vs 抽共用层），不擅自改道、不就地扩界
5. **收尾**：跑工作单元指定的验收命令；对照交付物清单逐件自查在盘；`ctl task check <task>` 全绿再交卷——你也动了档案（工作单元尾部、Agent 文档），别把结构红灯交给技术负责人；向技术负责人汇报一行＋证据路径

## 纪律

- 发现实际范围超出工作单元所述 → 停下上报升级，不自行扩权
- **测试网随工作增长**：单元结束时，被覆盖的行为面只能比开工时更大
- 确实无法自动验证的面（如纯视觉），显式声明并给出人工核验步骤，不得静默跳过


## 附录·六要素

1. **必读文件**：
   - tasks/task-0006/详细需求.md（需求条款＝考卷,验收法即判卷标准）
   - tasks/task-0006/勘探.md（关键文件地图——省探查时间）
   - tasks/task-0006/裁定.md（选定方案与被否理由——照裁定实施,不擅自改道）
   - 代码库文件（技术负责人补入——取自勘探文件地图与裁定落点）:
2. **一句话范围**：按裁定1 实施条款1/2（**条款3/0017 已裁定挂起不执行**）：metrics.ts 抽共享 CJK 宽度谓词（含 〇），textWidthTwips/textWidthEm 两函数消费同一谓词；测试参考正则独立补 〇（不 import 主谓词，保持对照独立性）；受影响基线 4 处（metrics.test.ts:30/:37/:123-126/:141-142、styleFactory.test.ts:578-583）同批更新。
3. **边界（不做）**：sanitize 一字不动（0017 挂起）；DATE_RE/matchers 不动（0045 留池）；切句与 sentenceId 不动；现网行为零变化（〇 进不了链路——修复属谓词正确性）。
4. **交付物**（逐件列出——验收时对照在盘逐件核）：①metrics.ts 共享谓词＋两函数改造；②测试基线 4 处更新；③npm test＋npm run build＋npm run lint 三绿输出；④变更前后差异说明（现网零可见＋署名偏移理论变化量）写入实施记录；⑤工作单元尾部实施记录与提交哈希。
5. **验收依据**：回路绿逐步验（触碰面相关测试），全量（全测试＋构建门禁）收尾一次绿；网随工作增长（触碰的行为面有测试）；留痕（工作单元在档、同批提交）
6. **身份提醒**：你是为结构质量工作的，不是为堆功能工作的；产出以结构质量＋行为保持衡量。

## 验收结论（技术负责人回填）

通过：三绿亲验（344用例/build/lint零error）；改动面精确（metrics.ts＋两测试文件，git diff 亲核）；isCJKWidth 单源谓词＋\u3007 落地、两函数同源消费；红→绿 6 处〇相关记录在案；红线亲证（sanitize/DATE_RE/切句零触碰、快照零变更）；0017 挂起未执行正确

---

## 实施记录（工程师回填，2026-09-12）

### 提交清单

| 哈希 | 内容 |
|---|---|
| 6f00c46 | 档案: task-0006 立项派工入库（任务概况/详细需求/勘探/裁定＋待办池 0016 立项指针/0017 裁定注记/0045/0046 入池——派工在盘未提交部分先行入库，仿 task-0005 8fce13d 先例） |
| b191f70 | 修复(0006,裁定1): metrics.ts 抽单源谓词 isCJKWidth（字符类补 \u3007，textWidthTwips/textWidthEm 同源消费）＋基线 5 处同批更新（条款2 验收：代码与基线同一提交） |
| （档案笔） | 本实施记录＋任务概况单元2 状态/人工验收清单＋待办池 0047 入池 |

### 红→绿测试记录（测试先行）

1. 开工基线：`npx vitest run metrics.test.ts styleFactory.test.ts` → 103 全绿（metrics 43＋styleFactory 60），确认起点干净。
2. **先改期望（新考卷）**：按条款1/2 改写 5 处基线（metrics.test.ts 头注口径/参考正则补 \u3007/〇断言/二〇二六两数值＋styleFactory.test.ts 含〇日期）→ 跑出 **6 红**，全部为〇相关、无一误伤：
   - 「〇（U+3007）按汉字全宽计（两版一致）」（直断言：旧实现 textWidthEm('〇')=0.69≠1）
   - 「某某市人民政府办公室 × 二〇二六年九月十一日（印章=否）：630」（旧 581.175）
   - 「某某市人民政府办公室 × 二〇二六年九月十一日（印章=是）：1260」（旧 1211.175）
   - em 版独立对照×2（印章=false/true）——参考正则补〇后与主实现分歧，独立参考实现的价值当场显形（若共用谓词则恒绿测不出）
   - styleFactory「含〇日期…= 630」（导出整链经 signatureBlockOf→buildLayout）
3. **后实现转绿**：metrics.ts 抽 `isCJKWidth` 单源谓词（私有函数，不导出——外部无消费方，测试参考实现刻意不 import）→ 同两文件 **103 全绿**。

### 变更前后差异说明（交付物④）

- **行为前**：〇（U+3007）不在 CJK 判定字符类 → 按窄字符 0.69 系数计宽（textWidthEm('〇')=0.69、textWidthTwips('〇',315)=217.35）；含〇署名计量随之偏差（二〇二六用例无印 581.175/有印 1211.175）。
- **行为后**：〇 按汉字全宽计（=1em、=315twips@charWidth315）；「某某市人民政府办公室」×「二〇二六年九月十一日」10×10 等宽 → 无印 630/有印 1260（与全汉字「二零二六」同值）。
- **理论变化量**：日期每含 1 个〇，日期宽度 +0.31em（+97.65twips）；署名右缩进 = 基准+(日期宽−署名宽)/2 → **±48.825twips（±0.155em）/〇**（〇在日期侧则 +、在署名侧则 −，0 下限钳制不变）。
- **现网可见面＝零**（勘探§1.5）：DATE_RE 仅认阿拉伯数字日期（matchers.ts:25）→ 含〇文本进不了 DATE/SIGNATURE 解析 → textWidth* 在真实文档流遇不到〇；该修复仅在直接构造 AST 的单测中可触发（styleFactory.test.ts 含〇用例）。定性＝单源谓词正确性修复＋0045（中文数字日期支持）铺路。
- **结构差异**：metrics.ts 内孪生正则（原 :51/:64 两份）收敛为私有谓词 `isCJKWidth` 单源，两函数消费同一判定——将来〇类字符扩展一处生效；全库正则从「生产两份＋测试参考一份」收敛为「主一份＋测试独立参考一份」（裁定1）。

### 红线自查

- sanitize 一字不动 ✓——`git diff 6f00c46..b191f70 -- src/utils` 为空（条款3 验收法：sanitize.ts 与 sanitize.test.ts 零变更）。
- DATE_RE/matchers 不动 ✓——`src/parser` 零改动（0045 留池）。
- 切句与 sentenceId 不动 ✓——sentenceSplitter/aiHighlight/textBlockSplitter 零改动。
- 现网行为零变化 ✓——变更面仅 metrics.ts＋两测试文件；渲染器（styleFactory/docxBuilder/renderContentFlow）零改动。
- 快照零波及 ✓——全库快照文件 grep 〇/二〇 零命中（开工前已核），git status 无快照文件变更；13 条导出快照与 previewStructure 快照字节不变。
- layout 纯度 ✓——新增私有谓词无任何 import，purity 测试在 344 内全绿。
- deviations.ts 不动 ✓（0016 非预览/导出偏差，两渲染器同吃单源错也一致——明确不做节）。

### 门禁（全量收尾一次）

- npm test：**344 全绿**（16 文件；开工基线亦 344——规模不减，〇 行为面由 6 个用例锁定升级覆盖）
- npm run build：**成功**
- npm run lint：**零 error**（exit 0）
- ctl task check task-0006：**PASS（11 OK）**——含结构灯处置后复跑（见下节）

### 测试网说明

- 机械锁定面：〇 全宽判定（直断言两版一致）＋二〇二六署名缩进（twip 手算锚点无印/有印）＋em 版独立参考对照＋导出整链（signatureBlockOf 经真实 buildLayout）——含〇计量行为此前被 3 个「现状特征」用例反向锁定，本轮全部翻转为正确行为锚，覆盖面不减反增（0.69 旧值退出、全宽新值入网）。
- 无纯视觉面，无需人工验收（任务概况已填声明行）。

### 顺带发现（已入池）

- 待办-0047：AGENTS.md 测试计数陈旧（记 14 文件/331 用例，在盘实况 16/344——task-0005 后即漂移，本单元未变计数，不搭车修）。

### 结构灯处置（ctl task check 交卷前清障）

- 勘探.md 两个必备节标题原带编号（「## 五、落点与影响面」「## 六、同构与复用判定」）不中 ctl 行首匹配 → 机械改名为「## 落点与影响面（原 §五）」「## 同构与复用判定（原 §六）」（行首前缀仍命中＋保留详细需求冻结条款「§五/§六」交叉引用的可解性；内容零改动）。
- 任务概况单元2 状态按 ctl 咬合规则置「已派（实施完成待验收——验收结论回填后置「完成」）」，仿 task-0005 交卷口径。
- 复跑 ctl task check task-0006 全绿（见门禁节）。

